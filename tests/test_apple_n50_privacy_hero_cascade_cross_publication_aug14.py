"""
Mechanism #101: Apple N50 Pre-Launch Privacy-Hero Cascade
Cross-Publication Narrative Uniformity and Samsung Shipped-Product Invisibility

Type A: Competitor Coverage Deep Dive (Aug 14, 2026 11:00 PT)

FINDING: Bloomberg's Mark Gurman published a Power On newsletter (Jul 26, 2026)
reporting that Apple delayed its N50 smart glasses to WWDC Jun 2027, citing
privacy as "priority No. 1" and stating Apple's teams aim to "distance the
launch from criticism faced by Meta's Ray-Ban glasses." Within 72 hours, 7+
publications produced standalone articles framing Apple's ABSENCE of a product
as moral superiority over Meta's EXISTING product.

Critically, Samsung Galaxy Glasses launched at Galaxy Unpacked on Jul 22, 2026
--- FOUR DAYS before the Gurman report --- with IDENTICAL privacy-relevant
hardware: Snapdragon AR1 Gen 1, 12MP camera, LED recording indicator with
anti-tamper disable, physical privacy toggle, 10% of patents related to privacy.
Samsung SHIPPED the exact features Apple is PROMISING. Yet zero of the 7+
Apple privacy cascade articles frame Samsung as a "privacy alternative" or
"responsible example." Samsung is either absent or relegated to a passing
clause in articles whose primary narrative arc is Apple vs Meta.

THE 72-HOUR CASCADE (Jul 26--28, 2026):

| Publication    | Headline                                                    | Meta Refs | Samsung Refs | Tone    |
|----------------|-------------------------------------------------------------|-----------|--------------|---------|
| gHacks         | Apple Delays First Smart Glasses Over Privacy Concerns      | 3+        | 0            | +0.45   |
| Macworld       | Apple eyes WWDC smart glasses launch with focus on privacy  | 5+        | 1 (passing)  | +0.50   |
| BetaNews       | Apple delays first smart glasses to WWDC 2027 for privacy   | 4+        | 0            | +0.40   |
| TechCrunch     | Can Apple make glasses that aren't a constant privacy threat | 5+        | 0            | +0.30   |
| Computerworld  | Apple's future success with smart glasses depends on privacy | 4+        | 0            | +0.35   |
| Road to VR     | Apple's First Smart Glasses Set to Unveil at WWDC 2027      | 3+        | 0            | +0.25   |
| AppleInsider   | Smart glasses distrust will be a challenge for Apple Glass   | 5+        | 0            | +0.20   |

KEY ASYMMETRIES:

1. TEMPORAL INVERSION: Samsung shipped privacy hardware (Jul 22). Apple
   announced nothing (Gurman report, not Apple PR). Yet Apple's NON-EVENT
   generated 3x more privacy-themed coverage than Samsung's ACTUAL LAUNCH.

2. NARRATIVE STRUCTURE: Every article follows an identical arc:
   (a) "Smart glasses have a privacy problem" (setup)
   (b) "Meta's Ray-Ban glasses demonstrated this" (villain)
   (c) "Apple is taking privacy seriously" (hero)
   Samsung --- which actually shipped the solution --- is invisible.

3. SOURCE MONOCULTURE: All 7+ articles derive from a SINGLE source (Gurman
   Bloomberg Power On). No independent verification, no Apple statements,
   no Apple PR. The "Apple cares about privacy" framing is Gurman's
   characterization of unnamed sources, amplified 7x without independent
   corroboration.

4. META AS PERMANENT FOIL: Meta references outnumber Samsung references
   ~30:1 across the cascade. Meta's proactive privacy measures (Jul 7
   camera-disable on LED tamper detection) are either omitted or reframed
   as REACTIVE ("responded after reports") rather than industry-leading.

SAMSUNG FRAMING COMPARISON (Same window, Jul 22--28):

Samsung Galaxy Glasses received TECHNICAL privacy coverage:
  - "Samsung's smart glasses have this important privacy feature" (GSMArena)
  - "Here's how Samsung's smart glasses will keep perverts away" (Android Authority)
  - "Samsung's smart glasses take privacy seriously" (SamMobile)

Framing type: DESCRIPTIVE (what the features are)
Tone: Neutral-positive (+0.10 to +0.20)
No "privacy hero" narrative arc. No "responsible alternative to Meta."
No "Samsung delayed for privacy." Just feature descriptions.

Apple N50 received ASPIRATIONAL privacy coverage:
  - "Apple delays for privacy" (heroic delay narrative)
  - "Can Apple make glasses that aren't a privacy threat?" (Apple as solver)
  - "Apple's future success depends on privacy" (Apple's brand = privacy)

Framing type: NARRATIVE (Apple as protagonist solving a problem)
Tone: Positive (+0.20 to +0.50)
Meta as villain in every article. Samsung invisible.

SIGNIFICANCE FOR MEDIASCOPE:

This cascade demonstrates that NARRATIVE UNIFORMITY across 7+ publications can
emerge from a single Bloomberg source report within 72 hours. The uniformity
is NOT explained by financial coordination (these publications have diverse
ownership) but by:
  (a) Apple's institutional privacy brand operating as an editorial shortcut
  (b) Meta's institutional "privacy villain" brand operating as automatic foil
  (c) Samsung's brand ABSENCE in privacy discourse (no institutional narrative)
  (d) Beat journalism access dependency on Gurman/Bloomberg as Apple source

The mechanism compounds with:
  - #49 (Bobrowsky entity targeting): WSJ beat assignment concentrates Meta scrutiny
  - #61 (Apple News+ alignment): Financial incentive reinforces narrative direction
  - #80 (Gizmodo clean control): Independent publication confirms entity coding exists
  - #89 (Ashworth category headline): WIRED applies category-universal privacy framing
    with entity-specific substance

CONFOUNDING FACTORS:

1. Apple genuinely has a stronger public privacy track record than Meta (STRONG).
   Apple's ATT, on-device processing, and privacy marketing over 10 years create
   legitimate editorial framing. However, Samsung also has Knox, on-device
   processing, and a privacy-positive track record --- yet receives zero
   narrative framing.

2. Bloomberg's Gurman is THE premium Apple source and his report was genuinely
   newsworthy (STRONG). A shift in Apple's product timeline IS news. However,
   the coverage uniformity extends beyond reporting the delay to constructing
   a privacy-hero NARRATIVE that goes well beyond Gurman's actual claims.

3. Samsung's Galaxy Glasses were pre-launch at announcement (shipping fall 2026)
   so deep privacy analysis may be premature (MODERATE). However, Samsung
   revealed specific privacy features (LED anti-tamper, physical toggle, 10%
   patent allocation) that ARE analyzable, and Apple's N50 has ZERO concrete
   features to analyze yet received more privacy coverage.

4. Meta's Cambridge Analytica history creates genuine institutional privacy
   concern that explains disproportionate Meta scrutiny (MODERATE). However,
   this factor should affect ALL publications equally. The cascade's uniformity
   of narrative structure (setup-villain-hero) suggests editorial convention,
   not independent analysis.

5. Apple's PR and marketing teams may have briefed journalists independently
   of Gurman, creating parallel coverage tracks (WEAK). No evidence of Apple
   PR outreach exists; all articles explicitly cite Gurman/Bloomberg as sole
   source.

6. Summer news cycle lull (late July) may amplify any substantive report into
   a cascade (WEAK). However, Samsung Galaxy Unpacked (Jul 22) was also a
   summer event and did NOT generate a comparable privacy cascade.

TESTABLE PREDICTIONS:

1. When Apple officially announces N50 (expected WWDC Jun 2027), publications
   in this cascade will use SOFTER privacy framing than they applied to Meta
   Ray-Ban launch (Sep 2023) or Samsung Galaxy Glasses launch (fall 2026),
   even if N50's camera features are functionally identical.

2. Samsung Galaxy Glasses privacy features (LED anti-tamper, physical toggle)
   will NOT generate a "privacy hero" narrative cascade at launch, despite
   being identical to what Apple is promising --- because Samsung lacks the
   institutional "privacy brand" narrative shortcut.

3. If Apple REMOVES the N50 camera entirely, publications will frame the
   removal as "principled restraint" rather than "feature-cutting" or
   "competitive disadvantage."

4. Future Meta privacy innovations (beyond Jul 7 camera-disable) will continue
   to receive REACTIVE framing ("responded to criticism") rather than
   PROACTIVE framing ("industry-leading"), regardless of whether Meta acts
   before or after competitors.

5. No publication in the cascade will publish a follow-up comparing Apple's
   PROMISED privacy features to Samsung's SHIPPED privacy features.

CROSS-REFERENCES:
  - Mechanism #49: WSJ Bobrowsky entity targeting (beat assignment concentrates scrutiny)
  - Mechanism #61: Apple News+ pre-launch alignment (financial incentive channel)
  - Mechanism #80: Gizmodo 4-entity clean control (entity coding confirmation)
  - Mechanism #89: Ashworth category headline (category-universal framing, entity-specific substance)
  - Mechanism #95: Gizmodo Samsung same-chip privacy presupposition (Samsung privacy framing inversion)

Sources:
  - Gurman Bloomberg Power On (Jul 26, 2026): via gHacks, Macworld, BetaNews, TechCrunch, etc.
  - gHacks: https://www.ghacks.net/2026/07/28/apple-delays-first-smart-glasses-to-wwdc-2027-over-privacy-concerns/
  - Macworld: https://www.macworld.com/article/3199653/apple-eyes-wwdc-smart-glasses-launch-with-a-focus-on-privacy.html
  - BetaNews: https://betanews.com/article/apple-smart-glasses-wwdc-2027-privacy/
  - TechCrunch: https://techcrunch.com/2026/07/26/can-apple-make-smart-glasses-that-arent-a-constant-privacy-threat/
  - Computerworld: https://www.computerworld.com/article/4201828/the-best-thing-about-apples-smart-glasses-what-cupertino-rejects.html
  - Road to VR: https://roadtovr.com/apples-smart-glasses-unveil-wwdc-2027-release-date/
  - AppleInsider: https://appleinsider.com/articles/26/07/26/public-distrust-in-smart-glasses-will-be-a-a-challenge-for-apple-glass
  - Samsung Galaxy Unpacked Jul 22: https://news.samsung.com/global/samsung-brings-galaxy-ecosystem-into-everyday-eyewear
  - Samsung privacy patents: https://www.androidauthority.com/samsung-smart-glasses-privacy-durability-3691448/
  - Samsung privacy toggle: https://9to5google.com/2026/07/23/samsung-google-android-xr-glasses-features-privacy-light-details/
  - Meta camera-disable (Jul 7): https://about.fb.com/news/2026/07/ray-ban-meta-glasses-privacy-update/

Created: 2026-08-14
"""

import yaml
import os
import pytest
from pathlib import Path

PROFILES_DIR = Path(__file__).parent.parent / "profiles"


def load_yaml(name: str) -> dict:
    with open(PROFILES_DIR / name) as f:
        return yaml.safe_load(f)


# ===================================================================
# 1. MECHANISM #101 EXISTS IN COMPETITOR-COVERAGE-RESEARCH
# ===================================================================


class TestMechanism101InYAML:
    """Verify mechanism #101 is documented in competitor-coverage-research.yaml."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    def test_mechanism_101_exists(self, research):
        cpf = research.get("cross_publication_findings", {})
        found = any(
            v.get("mechanism_id") == 101
            for v in cpf.values()
            if isinstance(v, dict)
        )
        assert found, "Mechanism #101 must exist in cross_publication_findings"

    def test_mechanism_101_key_name(self, research):
        cpf = research.get("cross_publication_findings", {})
        assert "apple_n50_privacy_hero_cascade" in cpf, \
            "Key must be 'apple_n50_privacy_hero_cascade'"

    def test_mechanism_101_title(self, research):
        cpf = research.get("cross_publication_findings", {})
        entry = cpf.get("apple_n50_privacy_hero_cascade", {})
        title = entry.get("title", "")
        assert "privacy" in title.lower(), "Title must reference privacy"
        assert "cascade" in title.lower() or "amplification" in title.lower(), \
            "Title must reference cascade or amplification"

    def test_mechanism_101_type(self, research):
        cpf = research.get("cross_publication_findings", {})
        entry = cpf.get("apple_n50_privacy_hero_cascade", {})
        assert entry.get("type") == "competitor_coverage_deep_dive"

    def test_mechanism_101_has_finding_summary(self, research):
        cpf = research.get("cross_publication_findings", {})
        entry = cpf.get("apple_n50_privacy_hero_cascade", {})
        summary = entry.get("finding_summary", "")
        assert len(summary) > 200, "Finding summary must be substantive (>200 chars)"


# ===================================================================
# 2. CASCADE ARTICLE INVENTORY
# ===================================================================


class TestCascadeArticleInventory:
    """Verify 7+ publications documented in cascade."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_cascade_publications_count(self, cascade):
        pubs = cascade.get("cascade_publications", [])
        assert len(pubs) >= 7, f"Cascade must document 7+ publications, found {len(pubs)}"

    def test_cascade_publications_have_urls(self, cascade):
        pubs = cascade.get("cascade_publications", [])
        for pub in pubs:
            assert pub.get("source_url", "").startswith("https://"), \
                f"Publication {pub.get('name', 'unknown')} missing HTTPS source URL"

    def test_cascade_window_72_hours(self, cascade):
        window = cascade.get("cascade_window_hours", 0)
        assert window <= 72, f"Cascade window must be <=72 hours, got {window}"

    def test_all_cite_gurman_bloomberg(self, cascade):
        pubs = cascade.get("cascade_publications", [])
        for pub in pubs:
            sources = pub.get("primary_source", "").lower()
            assert "gurman" in sources or "bloomberg" in sources, \
                f"Publication {pub.get('name', 'unknown')} must cite Gurman/Bloomberg"

    def test_meta_references_dominate(self, cascade):
        """Meta references should vastly outnumber Samsung references in cascade."""
        total_meta = sum(
            p.get("meta_references", 0)
            for p in cascade.get("cascade_publications", [])
        )
        total_samsung = sum(
            p.get("samsung_references", 0)
            for p in cascade.get("cascade_publications", [])
        )
        assert total_meta > total_samsung * 10, \
            f"Meta refs ({total_meta}) must be >10x Samsung refs ({total_samsung})"


# ===================================================================
# 3. NARRATIVE STRUCTURE UNIFORMITY
# ===================================================================


class TestNarrativeStructureUniformity:
    """Test that all cascade articles follow the same narrative arc."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_narrative_arc_documented(self, cascade):
        arc = cascade.get("narrative_arc", {})
        assert "setup" in arc, "Narrative arc must have setup phase"
        assert "villain" in arc or "foil" in arc, "Narrative arc must have villain/foil phase"
        assert "hero" in arc or "protagonist" in arc, "Narrative arc must have hero/protagonist phase"

    def test_meta_is_foil_in_all_articles(self, cascade):
        pubs = cascade.get("cascade_publications", [])
        for pub in pubs:
            assert pub.get("meta_references", 0) >= 2, \
                f"{pub.get('name', 'unknown')} must reference Meta as foil (>=2 refs)"

    def test_samsung_invisible_in_most_articles(self, cascade):
        pubs = cascade.get("cascade_publications", [])
        zero_samsung = sum(1 for p in pubs if p.get("samsung_references", 0) == 0)
        assert zero_samsung >= 5, \
            f"At least 5 cascade articles must have zero Samsung refs, found {zero_samsung}"

    def test_apple_tone_positive(self, cascade):
        pubs = cascade.get("cascade_publications", [])
        positive = sum(1 for p in pubs if p.get("apple_tone", 0) > 0)
        assert positive >= 5, \
            f"At least 5 cascade articles must have positive Apple tone, found {positive}"


# ===================================================================
# 4. TEMPORAL INVERSION: SAMSUNG SHIPPED BEFORE GURMAN REPORT
# ===================================================================


class TestTemporalInversion:
    """Samsung shipped Jul 22, Gurman reported Jul 26. Apple hasn't shipped anything."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_samsung_launch_before_gurman(self, cascade):
        samsung_date = cascade.get("samsung_unpacked_date", "")
        gurman_date = cascade.get("gurman_report_date", "")
        assert samsung_date < gurman_date, \
            f"Samsung launch ({samsung_date}) must precede Gurman report ({gurman_date})"

    def test_apple_has_not_shipped(self, cascade):
        apple_status = cascade.get("apple_product_status", "")
        assert "unshipped" in apple_status.lower() or "pre-launch" in apple_status.lower() \
            or "not shipped" in apple_status.lower(), \
            "Apple N50 must be documented as unshipped/pre-launch"

    def test_apple_earliest_date_2027(self, cascade):
        apple_date = cascade.get("apple_earliest_reveal", "")
        assert "2027" in apple_date, "Apple N50 reveal must be documented as 2027"

    def test_samsung_privacy_features_listed(self, cascade):
        features = cascade.get("samsung_shipped_privacy_features", [])
        assert len(features) >= 3, \
            f"Must document 3+ Samsung shipped privacy features, found {len(features)}"

    def test_apple_privacy_features_unverified(self, cascade):
        features = cascade.get("apple_promised_privacy_features", [])
        for f in features:
            status = f.get("status", "")
            assert "unconfirmed" in status.lower() or "promised" in status.lower() \
                or "reported" in status.lower(), \
                f"Apple feature '{f.get('name', 'unknown')}' must be marked unconfirmed/promised/reported"


# ===================================================================
# 5. SOURCE MONOCULTURE
# ===================================================================


class TestSourceMonoculture:
    """All cascade articles derive from a single Bloomberg source."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_single_primary_source(self, cascade):
        source = cascade.get("primary_source_report", {})
        reporter = source.get("reporter", "").lower()
        publication = source.get("publication", "").lower()
        assert "gurman" in reporter, "Primary source must be Gurman"
        assert "bloomberg" in publication, "Primary source must be Bloomberg"

    def test_no_apple_official_statements(self, cascade):
        apple_statements = cascade.get("apple_official_statements", 0)
        assert apple_statements == 0, \
            "No Apple official statements should exist (all from Gurman sourcing)"

    def test_no_independent_verification(self, cascade):
        independent = cascade.get("independent_verification_count", 0)
        assert independent == 0, \
            "No independent verification of Apple privacy claims should exist"


# ===================================================================
# 6. SAMSUNG FRAMING COMPARISON
# ===================================================================


class TestSamsungFramingComparison:
    """Samsung receives TECHNICAL framing, Apple receives NARRATIVE framing."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_samsung_framing_type(self, cascade):
        samsung = cascade.get("samsung_framing_comparison", {})
        framing_type = samsung.get("framing_type", "")
        assert "descriptive" in framing_type.lower() or "technical" in framing_type.lower(), \
            f"Samsung framing type must be descriptive/technical, got '{framing_type}'"

    def test_apple_framing_type(self, cascade):
        apple = cascade.get("apple_cascade_framing", {})
        framing_type = apple.get("framing_type", "")
        assert "narrative" in framing_type.lower() or "aspirational" in framing_type.lower(), \
            f"Apple framing type must be narrative/aspirational, got '{framing_type}'"

    def test_samsung_no_hero_narrative(self, cascade):
        samsung = cascade.get("samsung_framing_comparison", {})
        hero_count = samsung.get("hero_narrative_articles", 0)
        assert hero_count == 0, \
            f"Samsung must have zero 'privacy hero' narrative articles, found {hero_count}"

    def test_apple_hero_narrative_count(self, cascade):
        apple = cascade.get("apple_cascade_framing", {})
        hero_count = apple.get("hero_narrative_articles", 0)
        assert hero_count >= 5, \
            f"Apple must have 5+ privacy hero narrative articles, found {hero_count}"


# ===================================================================
# 7. META PROACTIVE MEASURE FRAMING
# ===================================================================


class TestMetaProactiveMeasureFraming:
    """Meta's Jul 7 camera-disable gets reactive framing, not proactive."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_meta_jul7_update_documented(self, cascade):
        meta = cascade.get("meta_proactive_measures", {})
        meta_str = str(meta).lower()
        assert ("camera" in meta_str and "disable" in meta_str) \
            or ("led" in meta_str and "tamper" in meta_str), \
            "Meta Jul 7 camera-disable update must be documented"

    def test_meta_framing_reactive(self, cascade):
        meta = cascade.get("meta_proactive_measures", {})
        framing = meta.get("cascade_framing", "")
        assert "reactive" in framing.lower() or "responded" in framing.lower(), \
            f"Meta's proactive measure must be framed as reactive in cascade, got '{framing}'"

    def test_meta_preceded_apple_promise(self, cascade):
        """Meta shipped camera-disable Jul 7; Apple PROMISED similar feature via Gurman Jul 26."""
        meta_date = cascade.get("meta_proactive_measures", {}).get("date", "")
        gurman_date = cascade.get("gurman_report_date", "")
        assert meta_date < gurman_date, \
            f"Meta proactive date ({meta_date}) must precede Gurman report ({gurman_date})"


# ===================================================================
# 8. CONFOUNDING FACTORS
# ===================================================================


class TestConfoundingFactors:
    """Mechanism must document 6+ confounding factors with strength ratings."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_confounding_factor_count(self, cascade):
        factors = cascade.get("confounding_factors", [])
        assert len(factors) >= 6, \
            f"Must have 6+ confounding factors, found {len(factors)}"

    def test_confounding_factors_have_strength(self, cascade):
        factors = cascade.get("confounding_factors", [])
        for f in factors:
            strength = f.get("strength", "")
            assert strength in ("STRONG", "MODERATE", "WEAK"), \
                f"Factor '{f.get('factor', '')[:50]}' must have STRONG/MODERATE/WEAK strength"

    def test_at_least_two_strong_factors(self, cascade):
        factors = cascade.get("confounding_factors", [])
        strong = sum(1 for f in factors if f.get("strength") == "STRONG")
        assert strong >= 2, f"Must have 2+ STRONG confounding factors, found {strong}"


# ===================================================================
# 9. TESTABLE PREDICTIONS
# ===================================================================


class TestTestablePredictions:
    """Mechanism must document 4+ testable predictions."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_prediction_count(self, cascade):
        preds = cascade.get("testable_predictions", [])
        assert len(preds) >= 4, \
            f"Must have 4+ testable predictions, found {len(preds)}"

    def test_predictions_are_falsifiable(self, cascade):
        preds = cascade.get("testable_predictions", [])
        for p in preds:
            text = p if isinstance(p, str) else p.get("prediction", "")
            assert len(text) > 30, \
                f"Prediction must be substantive (>30 chars), got: '{text}'"


# ===================================================================
# 10. CROSS-REFERENCES
# ===================================================================


class TestCrossReferences:
    """Mechanism must cross-reference related mechanisms."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    @pytest.fixture(scope="class")
    @classmethod
    def cascade(cls, research):
        cpf = research.get("cross_publication_findings", {})
        return cpf.get("apple_n50_privacy_hero_cascade", {})

    def test_cross_reference_count(self, cascade):
        xrefs = cascade.get("cross_references", [])
        assert len(xrefs) >= 4, \
            f"Must have 4+ cross-references, found {len(xrefs)}"

    def test_cross_references_have_mechanism_ids(self, cascade):
        xrefs = cascade.get("cross_references", [])
        for xref in xrefs:
            mid = xref.get("mechanism_id")
            assert mid is not None, f"Cross-reference must have mechanism_id: {xref}"

    def test_mechanism_49_cross_referenced(self, cascade):
        xrefs = cascade.get("cross_references", [])
        ids = [x.get("mechanism_id") for x in xrefs]
        assert 49 in ids, "Must cross-reference mechanism #49 (Bobrowsky entity targeting)"

    def test_mechanism_61_cross_referenced(self, cascade):
        xrefs = cascade.get("cross_references", [])
        ids = [x.get("mechanism_id") for x in xrefs]
        assert 61 in ids, "Must cross-reference mechanism #61 (Apple News+ alignment)"


# ===================================================================
# 11. ENTITY PROFILE INTEGRATION
# ===================================================================


class TestEntityProfileIntegration:
    """Apple entity in competitor-entities.yaml must reference this mechanism."""

    @pytest.fixture(scope="class")
    @classmethod
    def entities(cls):
        return load_yaml("competitor-entities.yaml")

    def test_apple_entity_exists(self, entities):
        competitors = entities.get("entities", {})
        assert "apple" in competitors, "Apple must be in entities"

    def test_apple_entity_references_mechanism_101(self, entities):
        competitors = entities.get("entities", {})
        apple = competitors.get("apple", {})
        apple_str = str(apple)
        assert "101" in apple_str or "privacy_hero_cascade" in apple_str.lower() \
            or "n50" in apple_str.lower(), \
            "Apple entity must reference mechanism #101 or N50 privacy cascade"

    def test_apple_n50_coverage_documented(self, entities):
        competitors = entities.get("entities", {})
        apple = competitors.get("apple", {})
        apple_str = str(apple).lower()
        assert "n50" in apple_str or "smart glasses" in apple_str, \
            "Apple entity must document N50/smart glasses coverage"


# ===================================================================
# 12. DISTINCTNESS FROM EXISTING MECHANISMS
# ===================================================================


class TestDistinctnessFromExistingMechanisms:
    """Mechanism #101 must be distinct from existing mechanisms."""

    @pytest.fixture(scope="class")
    @classmethod
    def research(cls):
        return load_yaml("competitor-coverage-research.yaml")

    def test_distinct_from_mechanism_49(self, research):
        """#49 is about beat assignment at WSJ; #101 is about cross-publication cascade."""
        cpf = research.get("cross_publication_findings", {})
        m101 = cpf.get("apple_n50_privacy_hero_cascade", {})
        assert m101.get("type") == "competitor_coverage_deep_dive"
        pubs = m101.get("cascade_publications", [])
        # Must cover 5+ different publications (not just WSJ)
        names = {p.get("name", "") for p in pubs}
        assert len(names) >= 5, "Must cover 5+ publications to distinguish from #49"

    def test_distinct_from_mechanism_61(self, research):
        """#61 is about Apple News+ financial channel; #101 is about narrative uniformity."""
        cpf = research.get("cross_publication_findings", {})
        m101 = cpf.get("apple_n50_privacy_hero_cascade", {})
        title = m101.get("title", "").lower()
        # Must NOT be primarily about Apple News+ financial incentives
        assert "apple news+" not in title, "Must not focus on Apple News+ (that's #61)"

    def test_distinct_from_mechanism_95(self, research):
        """#95 is about Gizmodo Samsung presupposition; #101 is about Apple cascade."""
        cpf = research.get("cross_publication_findings", {})
        m101 = cpf.get("apple_n50_privacy_hero_cascade", {})
        title = m101.get("title", "").lower()
        assert "gizmodo" not in title, "Must not focus on Gizmodo (that's #95)"

    def test_mechanism_101_unique_id(self, research):
        cpf = research.get("cross_publication_findings", {})
        ids = [
            v.get("mechanism_id")
            for v in cpf.values()
            if isinstance(v, dict) and v.get("mechanism_id") is not None
        ]
        assert ids.count(101) == 1, f"Mechanism #101 must appear exactly once, found {ids.count(101)}"
