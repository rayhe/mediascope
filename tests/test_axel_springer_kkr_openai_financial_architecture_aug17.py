"""
Mechanism #143: Axel Springer / KKR / OpenAI — Triple-Layer Financial Architecture
Behind Business Insider's Meta Smart Glasses Coverage

Type C: Financial Incentive Mapping (Aug 17, 2026 — Iteration #145)

KEY FINDING: Business Insider (owned by Axel Springer, majority-owned by KKR) has
a three-layer financial architecture creating structural incentive to cover Meta
adversarially while remaining silent on Samsung/Google smart glasses privacy:

LAYER 1 — OpenAI CONTENT LICENSING DEAL (Dec 2023):
- Axel Springer signed a multi-year content licensing deal with OpenAI (Dec 13, 2023)
- Value: "tens of millions of euros per year" (Bloomberg Law, Financial Times)
- Covers: Business Insider, Politico, Bild, Welt
- OpenAI is Meta's DIRECT competitor in AI models (GPT vs Llama)
- Deal gives Axel Springer content "favorable position" in ChatGPT search results
- Axel Springer CEO Döpfner called it "first of its kind" — early-mover advantage
  in OpenAI's content partnership strategy
- OpenAI trains future models (GPT-5+) on Axel Springer content
- Meta has ZERO comparable content licensing deal with Axel Springer

FINANCIAL INCENTIVE: OpenAI's success directly generates recurring revenue for
Axel Springer. Coverage that weakens Meta (OpenAI's competitor in AI) indirectly
benefits the OpenAI partnership's value proposition. Meta's Llama models threaten
the closed-model business that makes OpenAI's content licensing deals viable.

LAYER 2 — KKR MAJORITY OWNERSHIP:
- KKR acquired majority stake in Axel Springer (2019-2020, ~€6.8B deal)
- KKR launched Helix Digital Infrastructure: $10B with Nvidia + former AWS CEO
  Adam Selipsky (Jun 2026)
- KKR + Energy Capital Partners: $50B strategic AI infrastructure partnership (Oct 2024)
- KKR: $34B+ invested across 22+ digital infrastructure companies (data centers, fiber)
- KKR's AI infrastructure portfolio serves hyperscalers: Google, Amazon, Microsoft
  — ALL are Meta's competitors in AI/wearables/smart glasses
- KKR's digital infrastructure manages over 100 facilities globally with 12GW
  development pipeline

FINANCIAL INCENTIVE: KKR's core growth strategy depends on AI infrastructure
buildout. This buildout primarily serves Google, Amazon, and Microsoft — companies
directly competing with Meta in AI models and smart glasses. KKR's financial
success correlates with Meta competitor success.

LAYER 3 — GOOGLE SEARCH TRAFFIC DEPENDENCY:
- Business Insider depends heavily on Google Search for traffic referrals
- Google is the dominant search referrer for digital media
- Google competes with Meta in smart glasses (Samsung/Google Android XR glasses
  use identical hardware: Snapdragon AR1 Gen 1, 12MP camera, LED indicator)
- Adversarial Meta content generates more Google Search engagement (alarmist
  headlines index higher for privacy-related queries)
- Google's smart glasses partnership with Samsung makes negative Meta glasses
  coverage indirectly favorable to Google's competitive position

COVERAGE ASYMMETRY EVIDENCE:

Business Insider Meta smart glasses coverage (2025-2026):
- "Documents viewed by Business Insider reveal" internal "Early Adopter" program
  concerns (adversarial investigative sourcing)
- Testers noted feeling "creepy" wearing device (alarm vocabulary)
- "Surreptitious surveillance" framing
- "Formidable tool for surreptitious surveillance" (direct quote from BI reporting)
- Broke the Instagram pickup artist / smart glasses harassment story (Jul 2026):
  cited by PetaPixel, Gizmodo, NY Post, PR Daily, Analytics Insight
- "Pervert glasses" language propagated via BI reporting

Business Insider Samsung/Google smart glasses coverage (2025-2026):
- ZERO standalone Samsung/Google smart glasses privacy articles found
- ZERO alarm vocabulary applied to identical Samsung camera hardware
- Samsung glasses use SAME Snapdragon AR1 Gen 1 chip, SAME 12MP camera,
  SAME LED privacy indicator as Meta Ray-Ban
- Google glasses auto-activate camera with Gemini (MORE aggressive than Meta)
  — zero privacy coverage from BI

TRIPLE INCENTIVE ALIGNMENT:
- Layer 1 (OpenAI deal): Meta's Llama threatens OpenAI's business model →
  adversarial Meta coverage protects the deal's value
- Layer 2 (KKR ownership): KKR's AI infrastructure serves Meta competitors →
  Meta competitor success = KKR returns
- Layer 3 (Google traffic): Google search referrals fund BI's ad revenue →
  Google is Samsung/Google glasses partner

UNIQUE vs APOLLO MECHANISM (#111):
- Apollo owns Yahoo (TechCrunch, Engadget) — DUAL relationship (investment + ownership)
- KKR owns Axel Springer (Business Insider, Politico) — DUAL relationship
  (investment + ownership) + TRIPLE with OpenAI content deal
- Both PE firms have AI infrastructure portfolios serving Meta competitors
- Apollo: $38.5B direct AI deals, $40B+ broader digital infrastructure
- KKR: $34B+ digital infrastructure, $10B Helix, $50B ECP partnership
- NEITHER PE firm has comparable financial relationships with Meta

CONFOUNDING FACTORS:
1. STRONG: KKR and Axel Springer editorial operate independently; no evidence
   KKR directs Business Insider coverage decisions
2. STRONG: Business Insider journalists may have genuine editorial independence
   from parent company financial relationships; coverage could reflect authentic
   journalistic judgment about privacy risks
3. MODERATE: OpenAI content deal is non-exclusive; Axel Springer could sign
   similar deal with Meta (but hasn't)
4. MODERATE: Meta's actual privacy incidents (Swedish investigation, harassment
   videos) provide legitimate news hooks that Samsung lacks pre-launch
5. WEAK: KKR's AI infrastructure portfolio is diversified; not all facilities
   serve Meta competitors exclusively

FALSIFIABLE PREDICTIONS:
1. If Samsung launches its glasses (Fall 2026) with identical camera capabilities
   and users misuse them similarly, Business Insider will NOT produce the same
   volume or intensity of privacy alarm coverage as for Meta glasses
2. If OpenAI signs a competing deal with Meta for Llama model training data,
   Business Insider's adversarial Meta coverage intensity will decrease
3. If KKR divests from Axel Springer, Business Insider's Meta coverage will
   trend toward the median of tech publications not owned by PE firms with
   AI infrastructure portfolios

Sources:
  - OpenAI-Axel Springer deal announcement:
    https://techcrunch.com/2023/12/13/openai-inks-deal-with-axel-springer-on-licensing-news-for-model-training/
  - Reuters coverage of deal:
    https://www.reuters.com/business/media-telecom/global-news-publisher-axel-springer-partners-with-openai-landmark-deal-2023-12-13/
  - Bloomberg Law financial terms ("tens of millions"):
    https://news.bloomberglaw.com/tech-and-telecom-law/openai-to-pay-axel-springer-tens-of-millions-to-use-news-content
  - KKR Helix $10B launch:
    https://www.reuters.com/legal/transactional/kkr-launches-10-billion-ai-infrastructure-company-with-nvidia-vistra-2026-06-11/
  - KKR $50B AI partnership with ECP:
    https://investor.wedbush.com/wedbush/article/bizwire-2024-10-30-kkr-and-energy-capital-partners-announce-50-billion-strategic-partnership-to-support-ai-growth-through-investments-in-data-centers-and-power-generation
  - BI internal dissent article (via WebProNews citing BI):
    https://www.webpronews.com/through-the-looking-glass-internal-dissent-and-privacy-fears-haunt-metas-hardware-ambitions/
  - BI Instagram harassment ban report (via PetaPixel citing BI):
    https://petapixel.com/2026/07/27/instagram-is-banning-creepy-hidden-camera-videos-filmed-with-meta-smart-glasses/

Created: 2026-08-17
"""

import yaml
import os
import pytest

RESEARCH_PATH = os.path.join(
    os.path.dirname(__file__), '..', 'profiles', 'competitor-coverage-research.yaml'
)

ENTITIES_PATH = os.path.join(
    os.path.dirname(__file__), '..', 'profiles', 'competitor-entities.yaml'
)


@pytest.fixture(scope='module')
def research_data():
    with open(RESEARCH_PATH) as f:
        return yaml.safe_load(f)


@pytest.fixture(scope='module')
def entities_data():
    with open(ENTITIES_PATH) as f:
        return yaml.safe_load(f)


@pytest.fixture(scope='module')
def mechanism_143(research_data):
    cpf = research_data.get('cross_publication_findings', {})
    section = cpf.get('axel_springer_kkr_openai_financial_architecture')
    assert section is not None, (
        "Missing axel_springer_kkr_openai_financial_architecture in "
        "competitor-coverage-research.yaml cross_publication_findings"
    )
    return section


@pytest.fixture(scope='module')
def axel_springer_entity(entities_data):
    entity = entities_data.get('entities', {}).get('axel_springer_business_insider', {})
    assert entity, "Missing axel_springer_business_insider entity in competitor-entities.yaml"
    return entity


# ── Class 1: Mechanism Metadata ─────────────────────────────────────


class TestMechanismMetadata:
    """Verify mechanism #143 metadata is correctly documented."""

    def test_mechanism_id(self, mechanism_143):
        assert mechanism_143.get('mechanism_id') == 143

    def test_date_added(self, mechanism_143):
        assert mechanism_143.get('date_added') == '2026-08-17'

    def test_finding_type(self, mechanism_143):
        ft = mechanism_143.get('finding_type', '')
        assert 'financial' in ft.lower() or 'incentive' in ft.lower()

    def test_domain(self, mechanism_143):
        domain = mechanism_143.get('domain', '')
        assert 'business' in domain.lower() or 'axel' in domain.lower()

    def test_has_source_urls(self, mechanism_143):
        urls = mechanism_143.get('source_urls', [])
        assert len(urls) >= 5, f"Expected >=5 source URLs, got {len(urls)}"

    def test_has_test_file(self, mechanism_143):
        tf = mechanism_143.get('test_file', '')
        assert 'axel_springer' in tf


# ── Class 2: OpenAI Content Licensing Deal ──────────────────────────


class TestOpenAIContentDeal:
    """Verify OpenAI-Axel Springer content licensing deal documentation."""

    def test_openai_deal_exists(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal')
        assert deal is not None, "Missing openai_content_deal section"

    def test_deal_date(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        assert deal.get('announcement_date') == '2023-12-13'

    def test_deal_duration(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        duration = deal.get('duration', '')
        assert 'multi' in duration.lower() or 'year' in duration.lower()

    def test_deal_financial_scale(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        scale = deal.get('annual_value_description', '')
        assert 'tens of millions' in scale.lower() or 'eight-digit' in scale.lower()

    def test_publications_covered(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        pubs = deal.get('publications_covered', [])
        assert 'Business Insider' in pubs or 'business_insider' in [p.lower().replace(' ', '_') for p in pubs]

    def test_deal_includes_training_data(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        assert deal.get('includes_training_data') is True

    def test_chatgpt_integration(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        assert deal.get('chatgpt_summary_integration') is True

    def test_meta_has_no_comparable_deal(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        assert deal.get('meta_comparable_deal') is False or deal.get('meta_comparable_deal') is None

    def test_openai_competes_with_meta(self, mechanism_143):
        deal = mechanism_143.get('openai_content_deal', {})
        comp = deal.get('openai_meta_competitor_relationship', '')
        assert 'direct' in comp.lower() or 'compet' in comp.lower()


# ── Class 3: KKR Ownership and AI Infrastructure ────────────────────


class TestKKROwnership:
    """Verify KKR ownership structure and AI infrastructure portfolio."""

    def test_kkr_section_exists(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership')
        assert kkr is not None, "Missing kkr_ownership section"

    def test_kkr_majority_stake(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        assert kkr.get('ownership_type') in ['majority', 'controlling']

    def test_acquisition_year(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        year = kkr.get('acquisition_year')
        assert year in [2019, 2020], f"KKR acquisition year should be 2019 or 2020, got {year}"

    def test_helix_digital_infrastructure(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        helix = kkr.get('helix_digital_infrastructure')
        assert helix is not None

    def test_helix_committed_capital(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        helix = kkr.get('helix_digital_infrastructure', {})
        cap = helix.get('committed_capital_b')
        assert cap >= 10, f"Helix committed capital should be >= $10B, got {cap}"

    def test_helix_nvidia_partnership(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        helix = kkr.get('helix_digital_infrastructure', {})
        partners = helix.get('partners', [])
        partner_names = [p.lower() if isinstance(p, str) else '' for p in partners]
        assert any('nvidia' in p for p in partner_names)

    def test_helix_led_by_former_aws_ceo(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        helix = kkr.get('helix_digital_infrastructure', {})
        leader = helix.get('ceo', '')
        assert 'selipsky' in leader.lower()

    def test_total_digital_infrastructure_investment(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        total = kkr.get('total_digital_infrastructure_b')
        assert total >= 34, f"KKR total digital infra should be >= $34B, got {total}"

    def test_ecp_partnership(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        ecp = kkr.get('ecp_ai_partnership')
        assert ecp is not None

    def test_ecp_partnership_scale(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        ecp = kkr.get('ecp_ai_partnership', {})
        scale = ecp.get('total_b')
        assert scale >= 50, f"ECP partnership should be >= $50B, got {scale}"

    def test_hyperscaler_clients_are_meta_competitors(self, mechanism_143):
        kkr = mechanism_143.get('kkr_ownership', {})
        clients = kkr.get('hyperscaler_clients', [])
        client_names = [c.lower() if isinstance(c, str) else '' for c in clients]
        meta_competitors = ['google', 'amazon', 'microsoft']
        found = sum(1 for mc in meta_competitors if any(mc in c for c in client_names))
        assert found >= 2, f"Expected >=2 Meta competitor hyperscaler clients, found {found}"


# ── Class 4: Coverage Asymmetry Evidence ─────────────────────────────


class TestCoverageAsymmetry:
    """Verify Business Insider coverage asymmetry documentation."""

    def test_meta_coverage_section(self, mechanism_143):
        mc = mechanism_143.get('coverage_asymmetry', {}).get('meta_coverage')
        assert mc is not None

    def test_meta_alarm_terms_count(self, mechanism_143):
        mc = mechanism_143.get('coverage_asymmetry', {}).get('meta_coverage', {})
        terms = mc.get('alarm_terms', [])
        assert len(terms) >= 3, f"Expected >=3 Meta alarm terms, got {len(terms)}"

    def test_meta_investigative_sourcing(self, mechanism_143):
        mc = mechanism_143.get('coverage_asymmetry', {}).get('meta_coverage', {})
        sourcing = mc.get('investigative_sourcing', '')
        assert 'document' in sourcing.lower() or 'internal' in sourcing.lower()

    def test_samsung_google_coverage_section(self, mechanism_143):
        sg = mechanism_143.get('coverage_asymmetry', {}).get('samsung_google_coverage')
        assert sg is not None

    def test_samsung_google_zero_privacy_articles(self, mechanism_143):
        sg = mechanism_143.get('coverage_asymmetry', {}).get('samsung_google_coverage', {})
        count = sg.get('standalone_privacy_articles')
        assert count == 0, f"Expected 0 Samsung/Google standalone privacy articles, got {count}"

    def test_samsung_google_zero_alarm_terms(self, mechanism_143):
        sg = mechanism_143.get('coverage_asymmetry', {}).get('samsung_google_coverage', {})
        terms = sg.get('alarm_terms', [])
        assert len(terms) == 0, f"Expected 0 alarm terms for Samsung/Google, got {len(terms)}"

    def test_hardware_parity_documented(self, mechanism_143):
        hp = mechanism_143.get('coverage_asymmetry', {}).get('hardware_parity')
        assert hp is not None

    def test_same_chip(self, mechanism_143):
        hp = mechanism_143.get('coverage_asymmetry', {}).get('hardware_parity', {})
        chip = hp.get('shared_chip', '')
        assert 'ar1' in chip.lower() or 'snapdragon' in chip.lower()

    def test_same_camera_resolution(self, mechanism_143):
        hp = mechanism_143.get('coverage_asymmetry', {}).get('hardware_parity', {})
        cam = hp.get('shared_camera_mp')
        assert cam == 12


# ── Class 5: Triple Incentive Alignment ──────────────────────────────


class TestTripleIncentiveAlignment:
    """Verify the three-layer incentive alignment is documented."""

    def test_layers_documented(self, mechanism_143):
        layers = mechanism_143.get('incentive_layers', [])
        assert len(layers) >= 3, f"Expected >=3 incentive layers, got {len(layers)}"

    def test_layer_1_openai(self, mechanism_143):
        layers = mechanism_143.get('incentive_layers', [])
        layer_names = [l.get('name', '').lower() for l in layers]
        assert any('openai' in n for n in layer_names)

    def test_layer_2_kkr(self, mechanism_143):
        layers = mechanism_143.get('incentive_layers', [])
        layer_names = [l.get('name', '').lower() for l in layers]
        assert any('kkr' in n for n in layer_names)

    def test_layer_3_google_traffic(self, mechanism_143):
        layers = mechanism_143.get('incentive_layers', [])
        layer_names = [l.get('name', '').lower() for l in layers]
        assert any('google' in n or 'traffic' in n for n in layer_names)

    def test_each_layer_has_mechanism(self, mechanism_143):
        layers = mechanism_143.get('incentive_layers', [])
        for layer in layers:
            assert layer.get('mechanism') is not None, (
                f"Layer '{layer.get('name')}' missing mechanism description"
            )


# ── Class 6: Confounding Factors ─────────────────────────────────────


class TestConfounders:
    """Verify confounding factors are documented with appropriate strength."""

    def test_confounders_exist(self, mechanism_143):
        conf = mechanism_143.get('confounders', [])
        assert len(conf) >= 4, f"Expected >=4 confounders, got {len(conf)}"

    def test_has_strong_confounders(self, mechanism_143):
        conf = mechanism_143.get('confounders', [])
        strong = [c for c in conf if c.get('strength', '').lower() == 'strong']
        assert len(strong) >= 2, f"Expected >=2 STRONG confounders, got {len(strong)}"

    def test_editorial_independence_confounder(self, mechanism_143):
        conf = mechanism_143.get('confounders', [])
        editorial = [c for c in conf if 'editorial' in c.get('description', '').lower()
                     or 'independent' in c.get('description', '').lower()]
        assert len(editorial) >= 1, "Missing editorial independence confounder"

    def test_legitimate_news_hook_confounder(self, mechanism_143):
        conf = mechanism_143.get('confounders', [])
        legit = [c for c in conf if 'legitimate' in c.get('description', '').lower()
                 or 'incident' in c.get('description', '').lower()
                 or 'swedish' in c.get('description', '').lower()]
        assert len(legit) >= 1, "Missing legitimate news hook confounder"


# ── Class 7: Cross-References ────────────────────────────────────────


class TestCrossReferences:
    """Verify cross-references to related mechanisms."""

    def test_has_cross_references(self, mechanism_143):
        xrefs = mechanism_143.get('cross_references', [])
        assert len(xrefs) >= 2, f"Expected >=2 cross-references, got {len(xrefs)}"

    def test_references_apollo_mechanism(self, mechanism_143):
        xrefs = mechanism_143.get('cross_references', [])
        xref_ids = [x.get('mechanism_id') for x in xrefs]
        assert 111 in xref_ids, "Should cross-reference Apollo mechanism #111"

    def test_references_ziff_davis_mechanism(self, mechanism_143):
        """Axel Springer-Ziff Davis parallel: both are PE-owned media companies."""
        xrefs = mechanism_143.get('cross_references', [])
        xref_ids = [x.get('mechanism_id') for x in xrefs]
        # Should reference the Ziff Davis triple squeeze or another relevant mechanism
        has_relevant = any(mid in xref_ids for mid in [104, 111, 122, 128])
        assert has_relevant, "Should cross-reference at least one related mechanism"


# ── Class 8: Falsifiable Predictions ─────────────────────────────────


class TestFalsifiablePredictions:
    """Verify falsifiable predictions are documented."""

    def test_predictions_exist(self, mechanism_143):
        preds = mechanism_143.get('falsifiable_predictions', [])
        assert len(preds) >= 3, f"Expected >=3 predictions, got {len(preds)}"

    def test_samsung_launch_prediction(self, mechanism_143):
        preds = mechanism_143.get('falsifiable_predictions', [])
        pred_texts = [p.get('prediction', '').lower() for p in preds]
        assert any('samsung' in p and ('launch' in p or 'misuse' in p) for p in pred_texts)

    def test_predictions_are_falsifiable(self, mechanism_143):
        preds = mechanism_143.get('falsifiable_predictions', [])
        for pred in preds:
            assert pred.get('falsification_criteria') is not None, (
                f"Prediction missing falsification criteria: {pred.get('prediction', '')[:50]}"
            )


# ── Class 9: Entity Data ────────────────────────────────────────────


class TestEntityData:
    """Verify Axel Springer/Business Insider entity is in entities YAML."""

    def test_entity_exists(self, axel_springer_entity):
        assert axel_springer_entity is not None

    def test_entity_has_parent(self, axel_springer_entity):
        parent = axel_springer_entity.get('parent_company', '')
        assert 'kkr' in parent.lower() or 'axel' in parent.lower()

    def test_entity_has_openai_deal(self, axel_springer_entity):
        deals = axel_springer_entity.get('content_licensing_deals', [])
        deal_partners = [d.get('partner', '').lower() for d in deals]
        assert any('openai' in p for p in deal_partners)

    def test_entity_has_publications(self, axel_springer_entity):
        pubs = axel_springer_entity.get('publications', [])
        assert len(pubs) >= 2, f"Expected >=2 publications, got {len(pubs)}"

    def test_entity_mechanism_reference(self, axel_springer_entity):
        mechs = axel_springer_entity.get('mechanisms', [])
        mech_ids = [m.get('mechanism_id') for m in mechs]
        assert 143 in mech_ids
