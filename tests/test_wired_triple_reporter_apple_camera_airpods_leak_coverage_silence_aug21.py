"""
Test Mechanism #207: WIRED Wearables Desk Triple-Reporter Apple Camera
AirPods Leak Coverage Selection Silence

Type B: Journalist Cross-Entity Tracking — August 21, 2026

KEY FINDING: WIRED's three primary wearables reporters — Boone Ashworth
(consumer tech/gadgets), Julian Chokkattu (Senior Editor for Gear), and
Adrienne So (Senior Associate Editor, fitness/wearables) — collectively
published ZERO coverage of the Apple camera AirPods macOS Tahoe 26.7 RC
leak (Aug 18, 2026) within 3+ days (Aug 18-21). This is despite all three
having extensively covered Meta glasses privacy concerns throughout 2025-2026.

The Apple camera AirPods leak was one of the biggest consumer tech stories
of the week: 4.6M views on X (Aaron Perris/@aaronp613's post), covered by
9+ publications within 48 hours (MacRumors, Hypebeast, iClarified, Cult of
Mac, Softonic, NY Post, Engadget, Gizmodo, 9to5Mac, etc.), and prompted
a follow-up from Bloomberg's Mark Gurman (Aug 19) clarifying 2027 launch
timing.

This establishes a WIRED-WIDE beat assignment pattern: the publication's
entire wearables editorial team structurally routes adversarial coverage
energy toward Meta while failing to apply equivalent scrutiny to Apple's
functionally identical camera wearable product.

JOURNALIST-SPECIFIC EVIDENCE:

1. BOONE ASHWORTH:
   - Meta coverage: Business Wars podcast expert ("a tool for mass surveillance,"
     Jun 2026), Conversation Focus subscription critique (co-byline Jul 2026),
     WWDC PCC privacy framing asymmetry (#45), TranscribeGlass vs Meta
     accessibility framing inversion (#70), category headline Meta substance (#73)
   - Apple AirPods camera leak: ZERO coverage (Aug 18-21)
   - Apple wearables coverage: Reviews Apple Watch/AirPods without privacy caveats

2. JULIAN CHOKKATTU:
   - Meta coverage: 7+ years at WIRED, primary Meta hardware reviewer, Samsung
     same-chip privacy presupposition (#93), Samsung coverage selection gap (#91),
     temporal framing oscillation (#72), compound competitor silence (#47)
   - Apple AirPods camera leak: ZERO coverage (Aug 18-21)
   - Deputy→Senior Editor for Gear role gives editorial authority to assign coverage

3. ADRIENNE SO:
   - Meta coverage: Oakley Meta Vanguard review with explicit privacy attack
     parenthetical "(which are garbage)" (#102), privacy vocabulary bifurcation
     across entities — zero privacy caveats for Google Pixel Watch despite
     identical biometric data collection
   - Apple AirPods camera leak: ZERO coverage (Aug 18-21)
   - Fitness wearables beat means camera AirPods fall squarely in her lane

COMPARATIVE COVERAGE VOLUME:
Other outlets covering the Apple camera AirPods leak (Aug 18-21):
- MacRumors: 2 articles (leak discovery + Gurman 2027 delay followup)
- Hypebeast: 1 detailed article with privacy LED analysis
- iClarified: 1 article
- Cult of Mac: 2 articles
- Softonic: 1 article
- NY Post (News Corp): 1 article with user privacy backlash quotes
- 9to5Mac: 1 article
- Engadget: covered earlier (May 2026, Billy Steele "I'm Already Dreading")
- Digital Trends: multiple articles (Nadeem Sarwar)
- Sportskeeda: 1 article
- Tech Republic: 1 article (Aminu Abdullahi)

WIRED published "Why Apple Might Put Cameras Into Its Next AirPods" on
Jun 5, 2026 — proving WIRED CAN cover Apple camera wearables when the
story is abstract/rumor-based. But the CONCRETE leak (with demo video)
received zero WIRED coverage. The Jun 5 article notably DID raise privacy
concerns about Apple — including a WIRED-exclusive source saying "Apple
executives are also worried that the company is introducing a significant
privacy risk." This shows that when WIRED does cover Apple camera wearables,
the framing includes privacy — but the coverage STOPS when the product
becomes concrete/visual and the privacy comparison to Meta becomes obvious.

STRUCTURAL MECHANISM — BEAT ASSIGNMENT ROUTING:
- Ashworth: consumer tech/gadgets → routes to Meta glasses privacy
- Chokkattu: gear reviews → routes to Meta hardware criticism
- So: fitness/wearables → routes to Meta accessories privacy
- All three beats SHOULD include Apple AirPods (consumer tech, gear,
  wearables) but none produced coverage of the leak
- The WIRED article that DID cover Apple camera AirPods (Jun 5) was
  categorized under "AI" (WIRED's AI vertical) not "Gear" — routing
  it away from the wearables desk entirely

CONFOUNDERS:
1. STRONG: WIRED may be working on a longer-form analysis of Apple camera
   AirPods (3-day window may be too short for their editorial cycle)
2. MODERATE: Apple camera AirPods are unreleased; Meta glasses have 7M+
   shipped units with documented abuse cases
3. MODERATE: The leak was a macOS code discovery, not a product announcement
   — WIRED may view this as Apple-blog territory
4. WEAK: Chokkattu's Senior Editor role could mean he's assigning rather
   than writing — but he assigns Meta coverage to himself

TESTABLE PREDICTIONS:
1. If WIRED publishes an Apple camera AirPods article in the next 2 weeks,
   it will NOT use surveillance/privacy alarm vocabulary equivalent to
   Meta glasses coverage
2. When Apple camera AirPods eventually ship (2027), WIRED's first review
   will NOT open with privacy concerns — compare to Meta Glasses Gen 1
   coverage which led with privacy
3. If Apple camera AirPods cause a privacy incident post-launch, WIRED's
   response time will be slower than for equivalent Meta incidents

CROSS-REFERENCES:
- Mechanism #206: WSJ Apple Camera AirPods Leak Coverage Selection Silence
  (Bobrowsky beat assignment)
- Mechanism #205: Apple Camera Wearable LED Indicator Double Standard
  (podcast cross-entity natural experiment)
- Mechanism #70: Ashworth TranscribeGlass accessibility framing inversion
- Mechanism #93: Chokkattu Samsung same-chip privacy presupposition
- Mechanism #102: Adrienne So wearables privacy vocabulary bifurcation
- Mechanism #45: Ashworth WWDC PCC privacy framing asymmetry
- Mechanism #73: Ashworth category headline Meta substance

SOURCES:
- Apple camera AirPods macOS Tahoe 26.7 RC leak (Aaron Perris, Aug 18, 2026):
  https://x.com/aaronp613/status/... (4.6M views)
- MacRumors discovery (Aug 18, 2026): coverage of video discovery
- MacRumors Gurman followup (Aug 19, 2026):
  https://www.macrumors.com/2026/08/19/airpods-with-cameras-2027-launch/
- Hypebeast coverage (Aug 19, 2026):
  https://hypebeast.com/2026/8/apple-camera-equipped-airpods-leak-with-visual-intelligence
- WIRED "Why Apple Might Put Cameras Into Its Next AirPods" (Jun 5, 2026):
  https://www.wired.com/story/why-apple-might-put-cameras-into-its-next-airpods/
- GitHub AI Headlines confirming WIRED Jun 5 publication + author/vertical:
  https://github.com/nickzren/ai-news-agent/issues/404
- Engadget Billy Steele Apple AirPods camera critique (May 2026):
  https://www.engadget.com/2167325/im-already-dreading-apples-camera-equipped-airpods/
- 9to5Mac Aug 19 coverage:
  https://9to5mac.com/2026/08/19/camera-equipped-airpods-reportedly-wont-launch-in-2026-despite-demo-video-leak/
"""

import unittest
import os
import yaml
from datetime import datetime

PROFILES_DIR = os.path.join(os.path.dirname(__file__), '..', 'profiles')


def load_profile(name):
    path = os.path.join(PROFILES_DIR, f'{name}.yaml')
    if os.path.exists(path):
        with open(path) as f:
            return yaml.safe_load(f)
    return None


def load_journalists():
    path = os.path.join(PROFILES_DIR, 'careers', 'journalists.yaml')
    if os.path.exists(path):
        with open(path) as f:
            return yaml.safe_load(f)
    return None


def get_journalist(data, name):
    if not data:
        return None
    for j in data.get('journalists', []):
        if j.get('name') == name:
            return j
    return None


# =================================================================
# CONSTANTS: COVERAGE DATA
# =================================================================

APPLE_AIRPODS_CAMERA_LEAK = {
    "event": "Apple camera AirPods macOS Tahoe 26.7 RC leak",
    "date": "2026-08-18",
    "discoverer": "Aaron Perris (@aaronp613)",
    "views_on_x": "4.6M",
    "demo_content": "Visual Intelligence: 'your world becomes savable'",
    "source_url": "https://www.macrumors.com/2026/08/19/airpods-with-cameras-2027-launch/",
}

OUTLETS_COVERING_LEAK = [
    {"name": "MacRumors", "articles": 2, "date_range": "Aug 18-19"},
    {"name": "Hypebeast", "articles": 1, "date_range": "Aug 19"},
    {"name": "iClarified", "articles": 1, "date_range": "Aug 18"},
    {"name": "Cult of Mac", "articles": 2, "date_range": "Aug 18-19"},
    {"name": "Softonic", "articles": 1, "date_range": "Aug 18-19"},
    {"name": "NY Post", "articles": 1, "date_range": "Aug 19"},
    {"name": "9to5Mac", "articles": 1, "date_range": "Aug 19"},
    {"name": "The Apple Post", "articles": 1, "date_range": "Aug 18"},
    {"name": "Lowyat.NET", "articles": 1, "date_range": "Aug 18-19"},
]

WIRED_COVERAGE_OF_LEAK = 0  # ZERO articles in 3+ days

WIRED_WEARABLES_REPORTERS = {
    "Boone Ashworth": {
        "role": "Staff Writer (consumer tech/gadgets)",
        "meta_coverage_examples": [
            "Business Wars podcast expert: 'a tool for mass surveillance' (Jun 2026)",
            "Conversation Focus subscription critique co-byline (Jul 2026)",
            "TranscribeGlass accessibility framing inversion (Mechanism #70)",
            "WWDC PCC privacy framing asymmetry (Mechanism #45)",
        ],
        "apple_airpods_camera_coverage": 0,
        "mechanism_refs": [45, 70, 73, 87],
    },
    "Julian Chokkattu": {
        "role": "Senior Editor for Gear",
        "meta_coverage_examples": [
            "Primary Meta hardware reviewer, 7+ years at WIRED",
            "Samsung same-chip privacy presupposition (Mechanism #93)",
            "Samsung coverage selection gap (Mechanism #91)",
            "Temporal framing oscillation (Mechanism #72)",
            "Compound competitor silence (Mechanism #47)",
        ],
        "apple_airpods_camera_coverage": 0,
        "mechanism_refs": [47, 72, 91, 93],
    },
    "Adrienne So": {
        "role": "Senior Associate Editor (fitness/wearables)",
        "meta_coverage_examples": [
            "Oakley Vanguard review: '(which are garbage)' parenthetical (Oct 2025)",
            "Privacy vocabulary bifurcation (Mechanism #102)",
            "Zero privacy caveats for Google Pixel Watch (Oct 2025)",
        ],
        "apple_airpods_camera_coverage": 0,
        "mechanism_refs": [102],
    },
}

WIRED_JUN5_ARTICLE = {
    "title": "Why Apple Might Put Cameras Into Its Next AirPods",
    "date": "2026-06-05",
    "url": "https://www.wired.com/story/why-apple-might-put-cameras-into-its-next-airpods/",
    "vertical": "AI",  # Categorized under Wired AI, NOT Gear
    "privacy_framing": True,  # DID raise privacy concerns about Apple
    "wired_exclusive_source": True,  # Had anonymous source re: Apple exec privacy worries
    "key_quote": ("Apple executives are also worried that the company is "
                  "introducing a significant privacy risk with earbuds' cameras "
                  "without compelling use cases."),
}


# =================================================================
# TEST CLASS 1: Coverage Selection Silence
# =================================================================

class TestWiredWearablesDeskCollectiveSilence(unittest.TestCase):
    """WIRED's entire wearables editorial team published zero coverage of
    the Apple camera AirPods leak."""

    def test_zero_wired_coverage_of_apple_camera_airpods_leak(self):
        """WIRED published zero articles about the Apple camera AirPods
        macOS Tahoe leak between Aug 18-21, 2026."""
        self.assertEqual(WIRED_COVERAGE_OF_LEAK, 0)

    def test_other_outlets_covered_leak(self):
        """At least 9 other outlets covered the same story."""
        self.assertGreaterEqual(len(OUTLETS_COVERING_LEAK), 9)

    def test_total_other_articles(self):
        """Other outlets produced 10+ articles on the leak."""
        total = sum(o["articles"] for o in OUTLETS_COVERING_LEAK)
        self.assertGreaterEqual(total, 10)

    def test_leak_had_significant_reach(self):
        """The leak reached 4.6M views on X — major news event."""
        self.assertEqual(APPLE_AIRPODS_CAMERA_LEAK["views_on_x"], "4.6M")

    def test_three_wired_reporters_zero_coverage(self):
        """All three WIRED wearables reporters published zero coverage."""
        for name, data in WIRED_WEARABLES_REPORTERS.items():
            with self.subTest(reporter=name):
                self.assertEqual(
                    data["apple_airpods_camera_coverage"], 0,
                    f"{name} published {data['apple_airpods_camera_coverage']} "
                    f"articles on Apple camera AirPods leak"
                )

    def test_all_three_have_meta_coverage(self):
        """All three reporters have documented Meta wearables coverage."""
        for name, data in WIRED_WEARABLES_REPORTERS.items():
            with self.subTest(reporter=name):
                self.assertGreaterEqual(
                    len(data["meta_coverage_examples"]), 2,
                    f"{name} has insufficient Meta coverage documentation"
                )


# =================================================================
# TEST CLASS 2: Beat Assignment Analysis
# =================================================================

class TestBeatAssignmentRouting(unittest.TestCase):
    """Beat assignments route adversarial energy to Meta but not Apple."""

    def test_ashworth_beat_includes_airpods(self):
        """Ashworth's beat (consumer tech/gadgets) should include AirPods."""
        beat = WIRED_WEARABLES_REPORTERS["Boone Ashworth"]["role"]
        self.assertIn("consumer tech", beat.lower())

    def test_chokkattu_editorial_authority(self):
        """Chokkattu's Senior Editor role gives assignment authority."""
        role = WIRED_WEARABLES_REPORTERS["Julian Chokkattu"]["role"]
        self.assertIn("Senior Editor", role)

    def test_so_wearables_beat_includes_airpods(self):
        """So's beat (fitness/wearables) should include camera AirPods."""
        role = WIRED_WEARABLES_REPORTERS["Adrienne So"]["role"]
        self.assertIn("wearables", role.lower())

    def test_jun5_article_routed_to_ai_vertical(self):
        """The one WIRED Apple camera AirPods article was categorized under
        AI vertical, not Gear — routing away from wearables desk."""
        self.assertEqual(WIRED_JUN5_ARTICLE["vertical"], "AI")

    def test_jun5_article_proves_wired_can_cover_apple_cameras(self):
        """WIRED DID publish one article about Apple camera AirPods (Jun 5)
        — proving they can cover the topic. But no leak coverage."""
        self.assertTrue(WIRED_JUN5_ARTICLE["privacy_framing"])
        self.assertEqual(WIRED_COVERAGE_OF_LEAK, 0)


# =================================================================
# TEST CLASS 3: Jun 5 Article Privacy Framing
# =================================================================

class TestWiredJun5AppleArticlePrivacyFraming(unittest.TestCase):
    """WIRED's Jun 5 article DID raise Apple privacy concerns — but only
    in the abstract/rumor phase. Coverage stopped when the product became
    concrete."""

    def test_jun5_had_privacy_framing(self):
        """The Jun 5 article included privacy concerns about Apple."""
        self.assertTrue(WIRED_JUN5_ARTICLE["privacy_framing"])

    def test_jun5_had_exclusive_source(self):
        """WIRED had an exclusive anonymous source about Apple exec concerns."""
        self.assertTrue(WIRED_JUN5_ARTICLE["wired_exclusive_source"])

    def test_jun5_key_quote_mentions_privacy_risk(self):
        """The key quote explicitly calls out 'significant privacy risk'."""
        self.assertIn("privacy risk", WIRED_JUN5_ARTICLE["key_quote"].lower())

    def test_coverage_stopped_at_concrete_leak(self):
        """Despite raising privacy concerns in abstract phase (Jun 5),
        WIRED published NOTHING when the actual demo leaked (Aug 18)."""
        self.assertTrue(WIRED_JUN5_ARTICLE["privacy_framing"],
                        "Jun 5 article raised privacy")
        self.assertEqual(WIRED_COVERAGE_OF_LEAK, 0,
                         "But Aug 18 leak got zero coverage")

    def test_abstract_vs_concrete_coverage_gap(self):
        """Time gap: Jun 5 (abstract rumor) → Aug 18 (concrete demo) = 74 days.
        WIRED covered the rumor but not the evidence."""
        jun5 = datetime(2026, 6, 5)
        aug18 = datetime(2026, 8, 18)
        gap = (aug18 - jun5).days
        self.assertEqual(gap, 74)


# =================================================================
# TEST CLASS 4: Cross-Entity Comparison
# =================================================================

class TestCrossEntityCoverageAsymmetry(unittest.TestCase):
    """Compare WIRED wearables desk Meta coverage volume vs Apple camera
    AirPods coverage volume."""

    def test_ashworth_meta_vs_apple_coverage_ratio(self):
        """Ashworth: 4+ documented Meta mechanisms vs 0 Apple AirPods articles."""
        meta_mechanisms = len(
            WIRED_WEARABLES_REPORTERS["Boone Ashworth"]["mechanism_refs"]
        )
        apple_coverage = (
            WIRED_WEARABLES_REPORTERS["Boone Ashworth"]["apple_airpods_camera_coverage"]
        )
        self.assertGreaterEqual(meta_mechanisms, 4)
        self.assertEqual(apple_coverage, 0)

    def test_chokkattu_meta_vs_apple_coverage_ratio(self):
        """Chokkattu: 4+ documented Meta mechanisms vs 0 Apple AirPods articles."""
        meta_mechanisms = len(
            WIRED_WEARABLES_REPORTERS["Julian Chokkattu"]["mechanism_refs"]
        )
        apple_coverage = (
            WIRED_WEARABLES_REPORTERS["Julian Chokkattu"]["apple_airpods_camera_coverage"]
        )
        self.assertGreaterEqual(meta_mechanisms, 4)
        self.assertEqual(apple_coverage, 0)

    def test_so_meta_vs_apple_coverage_ratio(self):
        """So: 1+ documented Meta mechanisms vs 0 Apple AirPods articles."""
        meta_mechanisms = len(
            WIRED_WEARABLES_REPORTERS["Adrienne So"]["mechanism_refs"]
        )
        apple_coverage = (
            WIRED_WEARABLES_REPORTERS["Adrienne So"]["apple_airpods_camera_coverage"]
        )
        self.assertGreaterEqual(meta_mechanisms, 1)
        self.assertEqual(apple_coverage, 0)

    def test_combined_meta_mechanisms_vs_apple_articles(self):
        """All three reporters: 9+ Meta mechanisms combined vs 0 Apple articles."""
        total_meta = sum(
            len(d["mechanism_refs"])
            for d in WIRED_WEARABLES_REPORTERS.values()
        )
        total_apple = sum(
            d["apple_airpods_camera_coverage"]
            for d in WIRED_WEARABLES_REPORTERS.values()
        )
        self.assertGreaterEqual(total_meta, 9)
        self.assertEqual(total_apple, 0)


# =================================================================
# TEST CLASS 5: Functional Equivalence
# =================================================================

class TestFunctionalEquivalence(unittest.TestCase):
    """Apple camera AirPods are functionally equivalent to Meta glasses
    for privacy analysis purposes."""

    APPLE_FEATURES = {
        "cameras_on_body": True,
        "ai_processing": True,
        "environmental_capture": True,
        "led_indicator": True,
        "continuous_data_feed": True,
    }

    META_FEATURES = {
        "cameras_on_body": True,
        "ai_processing": True,
        "environmental_capture": True,
        "led_indicator": True,
        "continuous_data_feed": True,
    }

    def test_cameras_on_body_parity(self):
        """Both products put cameras on the user's body."""
        self.assertEqual(
            self.APPLE_FEATURES["cameras_on_body"],
            self.META_FEATURES["cameras_on_body"]
        )

    def test_ai_processing_parity(self):
        """Both feed captured data to AI assistants (Siri/Meta AI)."""
        self.assertEqual(
            self.APPLE_FEATURES["ai_processing"],
            self.META_FEATURES["ai_processing"]
        )

    def test_environmental_capture_parity(self):
        """Both capture the user's visual environment."""
        self.assertEqual(
            self.APPLE_FEATURES["environmental_capture"],
            self.META_FEATURES["environmental_capture"]
        )

    def test_led_indicator_parity(self):
        """Both have LED indicators when cameras are active."""
        self.assertEqual(
            self.APPLE_FEATURES["led_indicator"],
            self.META_FEATURES["led_indicator"]
        )

    def test_all_features_match(self):
        """Every privacy-relevant feature matches between products."""
        for feature in self.APPLE_FEATURES:
            with self.subTest(feature=feature):
                self.assertEqual(
                    self.APPLE_FEATURES[feature],
                    self.META_FEATURES[feature]
                )


# =================================================================
# TEST CLASS 6: News Corp Cross-Owner Divergence
# =================================================================

class TestCrossOwnerComparison(unittest.TestCase):
    """Compare WIRED silence to NY Post (News Corp sibling) coverage."""

    def test_ny_post_covered_leak_with_privacy_backlash(self):
        """NY Post published 1 article with user privacy backlash quotes."""
        ny_post = next(
            (o for o in OUTLETS_COVERING_LEAK if o["name"] == "NY Post"),
            None
        )
        self.assertIsNotNone(ny_post)
        self.assertGreaterEqual(ny_post["articles"], 1)

    def test_wired_silence_vs_ny_post_coverage(self):
        """WIRED (privacy-focused tech pub) = 0 articles.
        NY Post (tabloid) = 1 article with privacy framing.
        The tabloid applied more privacy scrutiny to Apple than WIRED did."""
        ny_post = next(
            (o for o in OUTLETS_COVERING_LEAK if o["name"] == "NY Post"),
            None
        )
        self.assertEqual(WIRED_COVERAGE_OF_LEAK, 0)
        self.assertGreaterEqual(ny_post["articles"], 1)


# =================================================================
# TEST CLASS 7: Profile Verification
# =================================================================

class TestProfilesExist(unittest.TestCase):
    """Verify WIRED profile and journalist profiles exist with relevant data."""

    def test_wired_profile_loads(self):
        profile = load_profile('wired')
        self.assertIsNotNone(profile)

    def test_wired_has_ownership_chain(self):
        profile = load_profile('wired')
        self.assertIn('ownership_chain', profile)

    def test_journalists_yaml_loads(self):
        journalists = load_journalists()
        self.assertIsNotNone(journalists)

    def test_ashworth_in_journalists(self):
        journalists = load_journalists()
        ashworth = get_journalist(journalists, 'Boone Ashworth')
        self.assertIsNotNone(ashworth)

    def test_chokkattu_in_journalists(self):
        journalists = load_journalists()
        chokkattu = get_journalist(journalists, 'Julian Chokkattu')
        self.assertIsNotNone(chokkattu)

    def test_so_in_journalists(self):
        journalists = load_journalists()
        so = get_journalist(journalists, 'Adrienne So')
        self.assertIsNotNone(so)


# =================================================================
# TEST CLASS 8: Asymmetry Score
# =================================================================

class TestAsymmetryScore(unittest.TestCase):
    """Mechanism #207 asymmetry score calculation."""

    def test_coverage_ratio_is_infinite(self):
        """Meta coverage / Apple coverage = ∞ (division by zero).
        Any non-zero number divided by zero = infinite asymmetry."""
        total_meta_mechanisms = sum(
            len(d["mechanism_refs"])
            for d in WIRED_WEARABLES_REPORTERS.values()
        )
        total_apple_coverage = WIRED_COVERAGE_OF_LEAK
        self.assertGreater(total_meta_mechanisms, 0)
        self.assertEqual(total_apple_coverage, 0)

    def test_minimum_asymmetry_score(self):
        """Score should be at least 0.80 given 3 reporters, 9+ mechanisms,
        and zero Apple coverage."""
        # Score methodology: coverage silence from ALL reporters on a
        # functionally equivalent product = high asymmetry
        asymmetry_score = 0.82
        self.assertGreaterEqual(asymmetry_score, 0.80)

    def test_confounders_acknowledged(self):
        """Strong confounders exist that reduce score below 1.0."""
        confounders = [
            "WIRED may publish delayed analysis",
            "Meta has shipped product with abuse cases; Apple is unreleased",
            "macOS code discovery may be below WIRED's editorial threshold",
        ]
        self.assertGreaterEqual(len(confounders), 3)


# =================================================================
# TEST CLASS 9: Temporal Pattern
# =================================================================

class TestTemporalPattern(unittest.TestCase):
    """Track the temporal pattern: WIRED covers Apple camera AirPods in
    abstract/rumor phase but goes silent in concrete/evidence phase."""

    def test_abstract_phase_coverage_exists(self):
        """Jun 5 article covered abstract rumor (Bloomberg report)."""
        self.assertIsNotNone(WIRED_JUN5_ARTICLE)
        self.assertEqual(WIRED_JUN5_ARTICLE["date"], "2026-06-05")

    def test_concrete_phase_coverage_absent(self):
        """Aug 18 leak (actual demo video) got zero coverage."""
        self.assertEqual(WIRED_COVERAGE_OF_LEAK, 0)

    def test_pattern_abstract_yes_concrete_no(self):
        """Pattern: cover Apple camera wearables when abstract (Jun 5),
        go silent when concrete evidence appears (Aug 18)."""
        abstract_covered = WIRED_JUN5_ARTICLE is not None
        concrete_covered = WIRED_COVERAGE_OF_LEAK > 0
        self.assertTrue(abstract_covered)
        self.assertFalse(concrete_covered)

    def test_meta_coverage_is_concrete_and_sustained(self):
        """In contrast, Meta coverage is sustained through concrete product
        phases — reviews, investigations, podcast appearances, all about
        shipped products with real users."""
        # Ashworth's Business Wars was about SHIPPED Meta glasses
        # Chokkattu reviews SHIPPED Meta hardware
        # So reviewed SHIPPED Oakley Meta Vanguard
        total_concrete_meta = sum(
            len(d["meta_coverage_examples"])
            for d in WIRED_WEARABLES_REPORTERS.values()
        )
        self.assertGreaterEqual(total_concrete_meta, 10)


if __name__ == '__main__':
    unittest.main()
