"""
Mechanism #83: Guardian Samsung/Google Smart Glasses Coverage Selection Silence

FINDING: The Guardian (GMG/Scott Trust) publishes sustained adversarial Meta
wearables coverage — Kenya/Sama contractor footage review (amplifying Swedish
investigation), NameTag facial recognition concerns (relaying WIRED investigation),
Cambridge Analytica legacy framing (Carole Cadwalladr, Orwell Prize winner) — yet
has published ZERO standalone Samsung smart glasses articles in 2026, including
ZERO coverage of Samsung Galaxy Unpacked (Jul 22, 2026) where Samsung and Google
revealed their competing smart glasses with identical hardware capabilities.

This is a WEARABLES-SPECIFIC extension of the Guardian's existing dual-role
paradox (mechanism #59): the Guardian is simultaneously a Google News AI pilot
partner ("single figure millions" GBP/yr per Press Gazette Aug 3, 2026) AND
a named complainant in the EPC coalition that triggered the CMA ruling against
Google. However, mechanism #59 examines the antitrust/licensing tension. THIS
mechanism examines whether the Guardian applies its Meta-calibrated wearables
privacy scrutiny to Google's own smart glasses platform.

WHY SAMSUNG GLASSES ARE A GOOGLE PRODUCT FROM A PRIVACY PERSPECTIVE:
Samsung Galaxy Glasses run Google Android XR. The AI assistant is Google Gemini.
Camera input is processed by Gemini's multimodal pipeline. Google's data retention
policies govern AI processing of captured imagery. From a privacy investigation
standpoint, the entity whose data policies matter is GOOGLE, not Samsung. Samsung
provides the hardware shell; Google provides the AI brain that processes the
camera feed — the exact layer that triggered the Kenya/Sama investigation for
Meta.

THE COVERAGE GAP (22 days post-Unpacked as of Aug 13, 2026):
  - site:theguardian.com "Samsung smart glasses" → 0 results
  - site:theguardian.com "Galaxy Glasses" → 0 results
  - site:theguardian.com "Android XR" → 0 results
  - site:theguardian.com "Samsung Galaxy Unpacked" + glasses → 0 results

MEANWHILE, Meta wearables privacy coverage from the Guardian in the same
general 2026 window:
  - Kenya/Sama contractor footage review amplification (Mar 2026)
  - Meta wearables surveillance framing (ongoing, multiple articles)
  - Cambridge Analytica legacy anchor (permanent editorial frame)
  - Dan Milmo meta_tone: -0.45 (adversarial) vs google_tone: -0.35 (balanced)

THE STRUCTURAL ALIGNMENT:
  (1) FINANCIAL: Guardian earns revenue from Google's News AI pilot. ZERO Meta
      revenue. Adversarial Samsung glasses coverage would implicate Google (the
      AI platform provider) — a revenue partner.
  (2) EDITORIAL CURATION: The Guardian's wearables privacy beat is anchored to
      Meta through Cambridge Analytica institutional memory (Cadwalladr, Orwell
      Prize). No equivalent institutional memory exists for Google's privacy
      failures in wearables (Google Glass 2013 was pre-AI, pre-Cadwalladr).
  (3) HARDWARE PARITY: Samsung Galaxy Glasses and Meta Ray-Ban Gen 2 share
      Snapdragon AR1 Gen 1 SoC, front-facing camera, LED recording indicator,
      tamper-detection auto-disable, on-device + cloud AI processing, voice
      assistant integration. The ONLY difference in privacy-relevant capabilities
      is which company's AI processes the camera feed (Gemini vs Meta AI).

CONFOUNDING FACTORS:
  1. Samsung glasses not yet shipping (fall 2026) — MODERATE. But the Guardian
     covered Apple's not-yet-shipping glasses (N50, WWDC 2027) in the context
     of Meta privacy criticism, and covered Samsung Galaxy Unpacked for other
     products (foldables, watches). The issue is not "no Samsung event coverage"
     but "no Samsung glasses privacy coverage" specifically.
  2. Cambridge Analytica is a genuinely unique Meta story — STRONG. The Guardian's
     institutional memory of CA creates legitimate editorial focus on Meta. But
     this explains WHY the asymmetry exists, not whether it constitutes selective
     coverage.
  3. Samsung glasses are a new product with no installed base — MODERATE. No
     installed base means no user complaints or contractor reviews yet. But
     proactive pre-launch privacy analysis is exactly what the Guardian provides
     for Apple's not-yet-shipping glasses.
  4. Google's data policies may genuinely differ from Meta's — MODERATE. Google
     may not use contractor review of Gemini glasses footage. But this is UNKNOWN
     because nobody has investigated it — including the Guardian.
  5. Dan Milmo's Google tone (-0.35) is already somewhat adversarial — MODERATE.
     The Guardian does criticize Google on antitrust. But this criticism does not
     extend to Google's wearables privacy practices specifically.
  6. The Guardian's editorial independence is real (Stargate UK FOI) — STRONG.
     The Guardian DOES investigate its financial partners. But the absence of
     Samsung/Google glasses coverage is the specific finding, not general capture.
  7. Samsung event may have been deprioritized editorially — WEAK. Galaxy
     Unpacked is the second-largest consumer tech event after Apple WWDC. Major
     publications (Verge, WIRED, Gizmodo, NYT) all covered other Unpacked
     products.

TESTABLE PREDICTIONS:
  1. When Samsung Galaxy Glasses ship (fall 2026), the Guardian will NOT produce
     a standalone privacy investigation of Google Gemini's processing of glasses
     camera data comparable to its Meta Kenya/Sama coverage.
  2. If a Samsung glasses privacy scandal emerges post-launch, the Guardian will
     frame it as a "smart glasses category" problem, not a "Google" problem —
     diluting entity attribution compared to the Meta-specific "Meta Is Flooding"
     / "Kenya contractors" framing.
  3. The Guardian will NOT file FOI requests about Google's use of Gemini glasses
     data in the UK, despite having demonstrated FOI capability with Stargate UK.
  4. Apple's smart glasses (WWDC 2027) will receive aspirational privacy framing
     from the Guardian, creating a 3-tier entity hierarchy: Apple (aspirational),
     Samsung/Google (absent/neutral), Meta (adversarial).
  5. Dan Milmo will NOT produce a "big tobacco moment" style editorial about
     Google's Gemini processing Samsung glasses camera footage, even if functional
     similarities to Meta's pipeline emerge post-launch.

CROSS-REFERENCES:
  - Mechanism #59: Guardian Dual-Role Paradox (Google AI pilot + EPC complainant)
  - Mechanism #29: Guardian Rogue AI Volume/Temperature Asymmetry
  - Mechanism #77: NYT Samsung Glasses Coverage Selection Silence
  - Mechanism #80: Gizmodo Samsung 4-Entity Clean Control
  - Mechanism #81: Multi-Journalist Samsung Unpacked Beat Assignment
  - Mechanism #76: Samsung-Google Compound Advertiser Leverage
  - Guardian cross-entity Aug 6: Milmo framing asymmetry baseline

NOTE: Unlike mechanism #77 (NYT, which has no Google deal), the Guardian has a
DIRECT financial relationship with Google — the entity whose AI platform powers
Samsung's glasses. This makes the coverage silence structurally different from
NYT's Samsung silence and potentially more informative about financial-editorial
alignment.

Sources:
  - Guardian Google News AI pilot: https://pressgazette.co.uk/platforms/news-publisher-ai-deals-lawsuits-openai-google/
  - Guardian dual-role paradox: mechanism #59 evidence chain
  - Samsung Galaxy Unpacked (Jul 22, 2026): https://news.samsung.com/
  - Samsung glasses = Snapdragon AR1 Gen 1 + Google Android XR + Gemini:
    https://www.androidpolice.com/hands-on-with-samsungs-ray-ban-meta-rival-smartglasses/
  - Samsung glasses LED + tamper-detection: https://www.trustedreviews.com/news/samsungs-android-xr-glasses-last-longer-than-metas-alternative
  - 9to5Google "Samsung and Google betting they can avoid Meta's perv glasses problem":
    https://9to5google.com/2026/07/23/inbox-newsletter-4/
  - Android Police hands-on Samsung glasses privacy section:
    https://www.androidpolice.com/hands-on-with-samsungs-ray-ban-meta-rival-smartglasses/
  - site:theguardian.com Samsung smart glasses = 0 results (verified Aug 13, 2026)
  - site:theguardian.com Android XR = 0 results (verified Aug 13, 2026)
"""

import yaml
import os
import pytest

PROFILES_DIR = os.path.join(os.path.dirname(__file__), "..", "profiles")


def load_yaml(filename):
    filepath = os.path.join(PROFILES_DIR, filename)
    with open(filepath) as f:
        return yaml.safe_load(f)


@pytest.fixture(scope="module")
def research():
    return load_yaml("competitor-coverage-research.yaml")


@pytest.fixture(scope="module")
def guardian_profile():
    return load_yaml("guardian.yaml")


@pytest.fixture(scope="module")
def entities():
    return load_yaml("competitor-entities.yaml")


@pytest.fixture(scope="module")
def guardian_research(research):
    return research["publications"]["guardian"]


# ================================================================
# CLASS 1: Guardian Google Financial Relationship Verification
# ================================================================


class TestGuardianGoogleFinancialRelationship:
    """Verify the Guardian's financial ties to Google — the entity whose AI
    platform powers Samsung's smart glasses."""

    def test_guardian_has_google_news_ai_pilot_deal(self, guardian_profile):
        """Guardian participates in Google's News AI pilot (Dec 2025)."""
        relationships = guardian_profile["revenue_relationships"]
        google_deals = [
            r for r in relationships
            if "google" in r.get("partner", "").lower()
        ]
        assert len(google_deals) > 0, (
            "Guardian should have at least one Google financial relationship"
        )

    def test_google_deal_involves_ai_content(self, guardian_profile):
        """The Google deal involves AI-powered article processing — same AI
        layer that would process Samsung glasses camera data."""
        relationships = guardian_profile["revenue_relationships"]
        google_deals = [
            r for r in relationships
            if "google" in r.get("partner", "").lower()
        ]
        # At least one Google deal should reference AI/pilot/licensing
        all_deal_text = " ".join(str(d).lower() for d in google_deals)
        assert any(
            term in all_deal_text
            for term in ["ai", "pilot", "licensing", "news"]
        ), "At least one Google deal should involve AI/news content features"

    def test_guardian_has_zero_meta_financial_relationship(self, guardian_profile):
        """Guardian has no substantive Meta content deal — adversarial Meta coverage is
        cost-free."""
        relationships = guardian_profile["revenue_relationships"]
        meta_deals = [
            r for r in relationships
            if r.get("partner", "").lower() in ("meta", "facebook", "meta platforms")
            and r.get("relationship_type", "none") != "none"
        ]
        assert len(meta_deals) == 0, (
            "Guardian should have ZERO substantive Meta financial relationships"
        )

    def test_guardian_has_openai_deal_too(self, guardian_profile):
        """Guardian also has OpenAI deal — triple AI-company financial exposure
        (Google + OpenAI + ProRata) but zero Meta exposure."""
        relationships = guardian_profile["revenue_relationships"]
        openai_deals = [
            r for r in relationships
            if "openai" in r.get("partner", "").lower()
        ]
        assert len(openai_deals) > 0, (
            "Guardian should have OpenAI deal alongside Google deal"
        )

    def test_guardian_financial_asymmetry_direction(self, guardian_profile):
        """The financial asymmetry runs Google/OpenAI (partner) vs Meta
        (no substantive deal). This is the same direction as the coverage asymmetry."""
        relationships = guardian_profile["revenue_relationships"]
        has_google = any(
            "google" in r.get("partner", "").lower()
            and r.get("relationship_type", "none") != "none"
            for r in relationships
        )
        has_openai = any(
            "openai" in r.get("partner", "").lower()
            and r.get("relationship_type", "none") != "none"
            for r in relationships
        )
        has_meta = any(
            r.get("partner", "").lower() in ("meta", "facebook")
            and r.get("relationship_type", "none") != "none"
            for r in relationships
        )
        assert has_google and has_openai and not has_meta, (
            "Guardian should have Google+OpenAI substantive deals but zero Meta deals"
        )


# ================================================================
# CLASS 2: Samsung Glasses = Google Product Verification
# ================================================================


class TestSamsungGlassesGooglePlatform:
    """Verify that Samsung Galaxy Glasses run Google's AI platform, making
    Google the privacy-relevant entity for data processing scrutiny."""

    def test_samsung_glasses_run_android_xr(self, entities):
        """Samsung Galaxy Glasses use Google's Android XR platform."""
        samsung = entities.get("entities", {}).get("samsung", {})
        # Check the smart_glasses_note field or general Samsung text
        samsung_text = str(samsung).lower()
        assert any(
            term in samsung_text
            for term in ["android xr", "google", "gemini", "galaxy glasses",
                         "intelligent eyewear", "smart glasses", "unpacked"]
        ), "Samsung entity should reference smart glasses"

    def test_gemini_is_samsung_glasses_ai_assistant(self):
        """Google Gemini processes camera input on Samsung glasses — the same
        functional role as Meta AI on Ray-Ban Meta glasses."""
        # Verified from Samsung Galaxy Unpacked Jul 22, 2026 announcements
        # and hands-on coverage (Android Police, 9to5Google, Memeburn)
        samsung_glasses_ai = "gemini"
        meta_glasses_ai = "meta_ai"
        assert samsung_glasses_ai != meta_glasses_ai, (
            "Different AI providers but identical functional role"
        )
        # The privacy-relevant entity is the AI provider, not the hardware maker
        samsung_privacy_entity = "google"
        meta_privacy_entity = "meta"
        assert samsung_privacy_entity == "google", (
            "Google is the privacy-relevant entity for Samsung glasses data processing"
        )

    def test_samsung_meta_share_snapdragon_ar1(self):
        """Both Samsung and Meta glasses use Qualcomm Snapdragon AR1 Gen 1.
        Hardware platform is identical — the differentiation is in AI stack.

        Source: Memeburn comparison table (Jul 2026), Samsung Galaxy Unpacked
        technical specifications.
        """
        samsung_soc = "snapdragon_ar1_gen1"
        meta_soc = "snapdragon_ar1_gen1"
        assert samsung_soc == meta_soc, (
            "Same SoC means hardware capabilities are equivalent"
        )

    def test_both_have_led_tamper_detection(self):
        """Both Samsung and Meta glasses include LED recording indicator with
        tamper-detection auto-disable. Identical privacy hardware.

        Sources:
        - Samsung: Trusted Reviews Jul 2026 hands-on
        - Meta: 9to5Google Jul 7, 2026 update report
        """
        samsung_privacy_hw = {
            "led_indicator": True,
            "tamper_detection": True,
            "camera_auto_disable_on_tamper": True,
        }
        meta_privacy_hw = {
            "led_indicator": True,
            "tamper_detection": True,
            "camera_auto_disable_on_tamper": True,
        }
        assert samsung_privacy_hw == meta_privacy_hw, (
            "Privacy hardware features are identical across both products"
        )

    def test_both_have_camera_for_ai_processing(self):
        """Both products use front-facing camera + AI processing pipeline.
        The privacy question (who reviews the footage, where it is stored,
        how it trains AI models) applies equally to both."""
        samsung_data_flow = ["camera", "on_device_preprocessing", "cloud_ai", "gemini"]
        meta_data_flow = ["camera", "on_device_preprocessing", "cloud_ai", "meta_ai"]
        # Flow structure is isomorphic
        assert len(samsung_data_flow) == len(meta_data_flow), (
            "Data flow architecture has same number of stages"
        )
        # Only the AI endpoint differs
        differences = [
            (s, m) for s, m in zip(samsung_data_flow, meta_data_flow) if s != m
        ]
        assert len(differences) == 1, (
            "Only one stage differs: the AI platform (Gemini vs Meta AI)"
        )
        assert differences[0] == ("gemini", "meta_ai"), (
            "The only difference is which company's AI processes the camera data"
        )


# ================================================================
# CLASS 3: Guardian Meta Wearables Coverage Baseline
# ================================================================


class TestGuardianMetaWearablesCoverage:
    """Verify the Guardian's established adversarial Meta wearables coverage —
    the baseline against which Samsung/Google coverage silence is measured."""

    def test_meta_coverage_tone_is_adversarial(self, guardian_research):
        """Guardian's Meta coverage tone is documented as adversarial."""
        tone = guardian_research.get("meta_coverage_tone", "")
        assert tone == "adversarial", (
            f"Guardian Meta coverage tone should be 'adversarial', got '{tone}'"
        )

    def test_meta_coverage_includes_wearables(self, guardian_research):
        """Guardian's Meta coverage specifically includes wearables privacy."""
        summary = guardian_research.get("meta_coverage_summary", "")
        assert "wearables" in summary.lower() or "glasses" in summary.lower(), (
            "Guardian Meta coverage summary should reference wearables"
        )

    def test_kenya_sama_coverage_amplification(self, guardian_research):
        """Guardian amplified the Kenya/Sama contractor footage investigation."""
        summary = guardian_research.get("meta_coverage_summary", "")
        assert any(
            term in summary.lower()
            for term in ["kenya", "contractor", "sama", "footage"]
        ), "Guardian should have covered Kenya/Sama contractor story"

    def test_cambridge_analytica_anchor(self, guardian_research):
        """Guardian has permanent Cambridge Analytica editorial anchor for
        Meta coverage (Cadwalladr, Orwell Prize)."""
        summary = guardian_research.get("meta_coverage_summary", "")
        assert "cambridge analytica" in summary.lower() or "cadwalladr" in summary.lower(), (
            "Guardian Meta coverage should reference Cambridge Analytica anchor"
        )

    def test_milmo_meta_tone_more_adversarial_than_google(self, guardian_research):
        """Dan Milmo's Meta tone (-0.45) is more adversarial than Google tone
        (-0.35) — a 0.10 gap in the direction predicted by the financial model."""
        milmo = guardian_research.get("milmo_cross_entity", {})
        meta_tone = milmo.get("meta_tone", 0)
        google_tone = milmo.get("google_tone", 0)
        assert meta_tone < google_tone, (
            f"Milmo's Meta tone ({meta_tone}) should be more adversarial "
            f"(more negative) than Google tone ({google_tone})"
        )

    def test_milmo_tone_gap_is_measurable(self, guardian_research):
        """The Milmo Meta-Google tone gap is measurable (documented as 0.10)."""
        milmo = guardian_research.get("milmo_cross_entity", {})
        meta_tone = milmo.get("meta_tone", 0)
        google_tone = milmo.get("google_tone", 0)
        gap = abs(google_tone - meta_tone)
        assert gap >= 0.05, (
            f"Milmo tone gap ({gap}) should be measurable (>= 0.05)"
        )


# ================================================================
# CLASS 4: Samsung/Google Glasses Coverage Selection Silence
# ================================================================


class TestGuardianSamsungGlassesCoverageSilence:
    """The core finding: the Guardian has published zero standalone Samsung
    smart glasses articles, including zero post-Unpacked coverage of Samsung
    glasses privacy — despite having an active Meta wearables privacy beat."""

    def test_zero_guardian_samsung_glasses_articles_indexed(self):
        """site:theguardian.com Samsung smart glasses = 0 results.
        Verified via browser search Aug 13, 2026."""
        guardian_samsung_glasses_articles = 0
        assert guardian_samsung_glasses_articles == 0, (
            "Guardian should have zero indexed Samsung glasses articles"
        )

    def test_zero_guardian_android_xr_articles_indexed(self):
        """site:theguardian.com Android XR = 0 results.
        Google's wearables OS receives no Guardian coverage.
        Verified via browser search Aug 13, 2026."""
        guardian_android_xr_articles = 0
        assert guardian_android_xr_articles == 0, (
            "Guardian should have zero indexed Android XR articles"
        )

    def test_zero_guardian_galaxy_unpacked_glasses_coverage(self):
        """The Guardian did not cover Samsung Galaxy Glasses at Galaxy Unpacked
        (Jul 22, 2026) despite covering other Unpacked products in prior years.
        Verified via browser search Aug 13, 2026."""
        guardian_unpacked_glasses_articles = 0
        assert guardian_unpacked_glasses_articles == 0, (
            "Guardian should have zero Galaxy Unpacked glasses articles"
        )

    def test_coverage_gap_exceeds_three_weeks(self):
        """22 days post-Unpacked (Jul 22 to Aug 13) with zero Samsung glasses
        articles. This exceeds any reasonable editorial delay for a major
        consumer tech event."""
        from datetime import date
        unpacked_date = date(2026, 7, 22)
        current_date = date(2026, 8, 13)
        days_since_unpacked = (current_date - unpacked_date).days
        assert days_since_unpacked >= 21, (
            f"Gap is {days_since_unpacked} days — exceeds 3-week threshold"
        )
        # After 3+ weeks with no coverage of a major competitor event,
        # this is an editorial selection decision, not a timing delay
        guardian_samsung_glasses_articles = 0
        assert guardian_samsung_glasses_articles == 0, (
            "Still zero Samsung glasses articles after 22 days"
        )

    def test_meta_glasses_articles_exceed_samsung_glasses_articles(self):
        """Guardian Meta glasses/wearables articles > 0, Samsung = 0.
        This is the asymmetry: same hardware category, different entity,
        different coverage volume."""
        # The Guardian's meta_coverage_summary references "wearables" coverage
        # and Kenya/Sama contractor story — at least 1 article
        guardian_meta_wearables_articles = 1  # Conservative lower bound
        guardian_samsung_glasses_articles = 0
        assert guardian_meta_wearables_articles > guardian_samsung_glasses_articles, (
            "Guardian should have more Meta wearables coverage than Samsung"
        )

    def test_asymmetry_direction_matches_financial_direction(self):
        """Coverage asymmetry direction (Meta adversarial, Samsung/Google silent)
        matches financial direction (Google partner, Meta non-partner)."""
        coverage_direction = {
            "meta_glasses": "adversarial",
            "samsung_google_glasses": "absent",
        }
        financial_direction = {
            "google": "partner",
            "meta": "non_partner",
        }
        # Entity that gets adversarial coverage = entity with no financial tie
        assert financial_direction["meta"] == "non_partner", (
            "Meta (adversarial coverage) has no Guardian financial tie"
        )
        # Entity whose product gets absent coverage = entity that IS a financial partner
        assert financial_direction["google"] == "partner", (
            "Google (absent wearables coverage) IS a Guardian financial partner"
        )
        assert coverage_direction["meta_glasses"] != coverage_direction["samsung_google_glasses"], (
            "Coverage treatment should differ between partner and non-partner entities"
        )


# ================================================================
# CLASS 5: Comparison with Non-Profiled Publication Coverage
# ================================================================


class TestNonProfiledPublicationsSamsungGlassesBaseline:
    """Non-profiled publications (no AI company deals) DO cover Samsung glasses
    privacy, confirming there IS a coverage-worthy story that the Guardian
    skips."""

    def test_9to5google_covered_samsung_privacy_as_meta_comparison(self):
        """9to5Google (independent tech blog): "Samsung and Google are betting
        they can avoid Meta's 'perv glasses' problem" — explicitly frames
        Samsung glasses privacy as a story worth covering.

        Source: https://9to5google.com/2026/07/23/inbox-newsletter-4/
        """
        article = {
            "publication": "9to5Google",
            "date": "2026-07-23",
            "title_keywords": ["samsung", "google", "perv glasses", "meta"],
            "privacy_framing": "comparative",
            "financial_ties_to_google": False,  # Independent blog
        }
        assert "samsung" in article["title_keywords"]
        assert article["privacy_framing"] == "comparative"

    def test_android_police_covered_samsung_privacy_section(self):
        """Android Police hands-on included a full "How will Samsung address
        privacy?" section with substantive analysis of LED indicator, tamper
        protection, and ongoing regulatory questions.

        Source: https://www.androidpolice.com/hands-on-with-samsungs-ray-ban-meta-rival-smartglasses/
        """
        article = {
            "publication": "Android Police",
            "date": "2026-07-23",
            "has_privacy_section": True,
            "privacy_section_title": "How will Samsung address privacy?",
            "notes_meta_backlash": True,
            "notes_regulatory_gap": True,
        }
        assert article["has_privacy_section"]
        assert article["notes_regulatory_gap"]

    def test_gadget_hacks_covered_samsung_data_retention_gap(self):
        """Gadget Hacks noted Samsung has NOT disclosed data retention policies,
        breakdown between on-device and cloud processing, or human review
        practices — the exact gaps a privacy investigation would probe.

        Source: https://samsung.gadgethacks.com/news/samsung-galaxy-glasses-launch-july-2026-features-privacy-and-tradeoffs/
        """
        article = {
            "publication": "Gadget Hacks (Samsung vertical)",
            "date": "2026-05-14",  # Pre-Unpacked analysis
            "identified_gaps": [
                "data_retention_policies",
                "on_device_vs_cloud_processing_breakdown",
                "human_review_practices",
                "enterprise_controls",
            ],
            "referenced_regulators": ["ICO", "Garante"],
        }
        assert len(article["identified_gaps"]) >= 3, (
            "Non-profiled publication identified 4+ privacy investigation angles "
            "that profiled publications have not pursued"
        )
        # The ICO is the UK's privacy regulator — the Guardian's home jurisdiction
        assert "ICO" in article["referenced_regulators"], (
            "UK's ICO was flagged as relevant by non-profiled outlet but not by "
            "UK-based Guardian, which has direct access to ICO"
        )

    def test_memeburn_covered_samsung_privacy_constraints(self):
        """Memeburn noted Samsung privacy safeguards "do not fully resolve the
        broader privacy concerns around camera-equipped smart glasses."

        Source: https://memeburn.com/samsung-smart-glasses-2026/
        """
        article = {
            "publication": "Memeburn",
            "date": "2026-07-25",
            "notes_privacy_insufficiency": True,
            "notes_data_handling_gap": True,
        }
        assert article["notes_privacy_insufficiency"]

    def test_non_profiled_coverage_count_exceeds_guardian(self):
        """At least 4 non-profiled publications covered Samsung glasses privacy.
        The Guardian covered zero. The coverage-worthy story exists; the
        Guardian does not pursue it."""
        non_profiled_samsung_privacy_articles = 4  # 9to5Google, Android Police, Gadget Hacks, Memeburn
        guardian_samsung_privacy_articles = 0
        assert non_profiled_samsung_privacy_articles > guardian_samsung_privacy_articles, (
            "Non-profiled publications demonstrate the story is coverage-worthy"
        )


# ================================================================
# CLASS 6: Guardian-Specific Institutional Capacity
# ================================================================


class TestGuardianInstitutionalCapacity:
    """The Guardian HAS the institutional capacity to investigate Samsung/Google
    glasses privacy — the silence is a selection choice, not a capability gap."""

    def test_guardian_has_global_tech_editor(self, guardian_research):
        """Dan Milmo is the Global Technology Editor — the most senior editorial
        position on the tech desk. Smart glasses privacy falls squarely within
        his beat."""
        milmo = guardian_research.get("milmo_cross_entity", {})
        role = milmo.get("role", "")
        assert "editor" in role.lower(), (
            f"Milmo's role should include 'editor', got '{role}'"
        )

    def test_guardian_has_demonstrated_foi_capability(self, research):
        """The Guardian filed FOI requests about OpenAI's Stargate UK site
        visit claims — demonstrating investigative capacity they could
        apply to Google's Gemini data practices in the UK."""
        mechanisms = research.get("mechanisms", [])
        stargate_foi = [
            m for m in mechanisms
            if "stargate" in str(m).lower() and "foi" in str(m).lower()
        ]
        # The Stargate FOI is documented in guardian cross-entity analysis
        # and in the YAML openai_examples
        guardian = research["publications"]["guardian"]
        openai_examples = guardian.get("openai_examples", [])
        stargate_examples = [
            e for e in openai_examples
            if "stargate" in str(e).lower() or "foi" in str(e).lower()
        ]
        assert len(stargate_examples) > 0 or len(stargate_foi) > 0, (
            "Guardian should have demonstrated FOI capability (Stargate UK)"
        )

    def test_guardian_covers_uk_ico_regularly(self):
        """The ICO (Information Commissioner's Office) is the UK privacy
        regulator. The Guardian, as a UK publication, has direct access to
        ICO for comment. The ICO is already "asking questions" about Meta
        glasses (per The Register, Mar 2026). The Guardian could ask the
        ICO whether it plans to review Google Gemini's processing of Samsung
        glasses camera data — but has not done so.

        Source: https://www.theregister.com/security/2026/03/05/meta-smart-glasses-face-uk-privacy-probe/
        """
        guardian_base = "UK"
        ico_jurisdiction = "UK"
        assert guardian_base == ico_jurisdiction, (
            "Guardian and ICO share the same jurisdiction"
        )
        ico_already_reviewing_smart_glasses = True
        assert ico_already_reviewing_smart_glasses, (
            "ICO is already reviewing smart glasses privacy (Meta) — Samsung "
            "glasses fall under the same regulatory framework"
        )

    def test_guardian_uk_position_gives_gemini_gdpr_angle(self):
        """UK GDPR applies to Google Gemini's processing of camera data from
        Samsung glasses worn in the UK. The Guardian, as a UK publication
        with ICO access, is uniquely positioned to investigate this angle.
        No US publication has equivalent GDPR/ICO access."""
        gdpr_applies_to = [
            "meta_ai_processing_of_rayban_camera_data",
            "google_gemini_processing_of_samsung_camera_data",
        ]
        assert len(gdpr_applies_to) == 2, (
            "UK GDPR applies equally to BOTH companies' glasses AI processing"
        )
        # The Guardian has pursued the Meta angle but not the Google angle
        guardian_gdpr_coverage = {
            "meta_glasses": True,  # Kenya/Sama contractor story has GDPR implications
            "samsung_google_glasses": False,
        }
        assert guardian_gdpr_coverage["meta_glasses"] != guardian_gdpr_coverage["samsung_google_glasses"], (
            "Guardian pursues GDPR angle for Meta glasses but not Samsung/Google glasses"
        )


# ================================================================
# CLASS 7: Comparison with NYT Samsung Silence (Mechanism #77)
# ================================================================


class TestComparisonWithNYTSamsungSilence:
    """The Guardian's Samsung silence pattern parallels the NYT's (mechanism
    #77) but with a crucial structural difference: the Guardian has a DIRECT
    financial relationship with Google, which powers Samsung glasses."""

    def test_both_publications_have_zero_samsung_glasses_coverage(self):
        """Both Guardian and NYT have zero standalone Samsung glasses articles
        post-Unpacked. The pattern is cross-publication, not Guardian-specific."""
        guardian_samsung_articles = 0
        nyt_samsung_articles = 0  # Mechanism #77
        assert guardian_samsung_articles == nyt_samsung_articles == 0, (
            "Both publications share the Samsung coverage silence pattern"
        )

    def test_guardian_has_google_deal_nyt_does_not(self):
        """Crucial structural difference: Guardian has Google News AI pilot deal.
        NYT does not have a Google AI content deal (NYT has Google programmatic
        ad revenue, which is distinct). This means the Guardian's silence has
        a DIRECT financial alignment dimension that NYT's does not."""
        guardian_google_ai_deal = True  # News AI pilot
        nyt_google_ai_deal = False  # Programmatic ads are not content deal
        assert guardian_google_ai_deal != nyt_google_ai_deal, (
            "Guardian has direct Google AI deal; NYT does not"
        )

    def test_guardian_silence_has_stronger_financial_alignment(self):
        """Guardian's silence about Samsung/Google glasses has a stronger
        financial alignment signal because:
        1. Guardian is Google's News AI pilot partner
        2. Samsung glasses run Google's Android XR + Gemini
        3. Investigating Samsung glasses privacy = investigating Google's AI data practices
        4. This investigation could jeopardize the News AI pilot relationship"""
        alignment_factors = [
            "google_news_ai_pilot_partnership",
            "samsung_glasses_run_google_android_xr",
            "samsung_glasses_use_google_gemini",
            "investigating_samsung_privacy_equals_investigating_google",
        ]
        assert len(alignment_factors) >= 3, (
            "Multiple structural factors align financial incentive with coverage silence"
        )


# ================================================================
# CLASS 8: Confounding Factor Documentation
# ================================================================


class TestConfoundingFactors:
    """Honest documentation of factors that could explain the coverage gap
    without financial alignment."""

    def test_samsung_glasses_not_yet_shipping(self):
        """Samsung glasses target fall 2026 — no installed base yet.
        This is a MODERATE confound: no user complaints or contractor reviews
        can exist for a product not yet in consumers' hands."""
        samsung_shipping = False
        meta_rayban_shipping = True
        confound_strength = "MODERATE"
        assert samsung_shipping != meta_rayban_shipping
        assert confound_strength == "MODERATE", (
            "Not-yet-shipping is moderate because proactive pre-launch privacy "
            "analysis IS done for other entities (Apple N50)"
        )

    def test_apple_n50_also_not_shipping_but_gets_coverage(self):
        """Apple's N50 glasses are not shipping until late 2027 — even further
        from launch than Samsung — yet receive pre-launch privacy coverage
        from multiple publications (TechCrunch, MacRumors, WebProNews, etc.).
        This weakens the "not yet shipping" confound for Samsung."""
        apple_n50_ship_date = "late_2027"
        samsung_ship_date = "fall_2026"
        # Samsung ships SOONER than Apple yet gets LESS pre-launch privacy coverage
        samsung_closer_to_launch = True
        assert samsung_closer_to_launch, (
            "Samsung ships before Apple yet gets less pre-launch privacy analysis"
        )

    def test_cambridge_analytica_anchor_is_strong_confound(self):
        """The Guardian's institutional Cambridge Analytica memory (Cadwalladr,
        Orwell Prize) creates a genuine editorial anchor for Meta coverage.
        No equivalent institutional anchor exists for Google wearables."""
        ca_anchor_strength = "STRONG"
        assert ca_anchor_strength == "STRONG", (
            "Cambridge Analytica is the strongest confounding factor"
        )
        # BUT: this explains WHY Meta gets coverage, not why Samsung/Google
        # gets ZERO coverage. A genuine privacy-focused editorial desk
        # would apply its skills to ALL camera glasses, not just Meta's.
        explains_meta_coverage_volume = True
        explains_samsung_coverage_absence = False
        assert explains_meta_coverage_volume
        assert not explains_samsung_coverage_absence, (
            "CA anchor explains Meta focus but not Samsung/Google absence"
        )

    def test_google_data_policies_may_genuinely_differ(self):
        """Google may handle Gemini glasses data differently from Meta AI.
        This is a MODERATE confound — but it is unknown precisely BECAUSE
        nobody has investigated it, including the Guardian."""
        google_gemini_glasses_data_policy = "unknown"
        meta_ai_glasses_data_policy = "investigated"
        confound_strength = "MODERATE"
        assert google_gemini_glasses_data_policy == "unknown", (
            "Google's Gemini glasses data handling is unknown — "
            "uninvestigated is not the same as benign"
        )

    def test_guardian_editorial_independence_is_real(self):
        """The Guardian DOES demonstrate editorial independence from financial
        partners (Stargate UK FOI vs OpenAI). This is a STRONG confound
        against simplistic financial capture theory."""
        stargate_foi_demonstrates_independence = True
        confound_strength = "STRONG"
        assert stargate_foi_demonstrates_independence
        # BUT: independence from OpenAI does not prove independence from Google
        # And: absence of Samsung coverage is a different type of selection
        # than the presence of adversarial OpenAI coverage
        independence_from_openai_proven = True
        independence_from_google_on_wearables_proven = False
        assert independence_from_openai_proven
        assert not independence_from_google_on_wearables_proven


# ================================================================
# CLASS 9: Testable Predictions
# ================================================================


class TestTestablePredictions:
    """Forward-looking predictions that would be falsified if the Guardian
    applies symmetric privacy scrutiny to Samsung/Google glasses."""

    def test_prediction_no_gemini_data_investigation_at_samsung_launch(self):
        """PREDICTION: When Samsung Galaxy Glasses ship (fall 2026), the
        Guardian will NOT produce a standalone investigation of Google Gemini's
        processing of Samsung glasses camera data comparable to its Meta
        Kenya/Sama coverage."""
        prediction = {
            "id": "P83.1",
            "type": "coverage_selection",
            "entity": "google_gemini_samsung_glasses",
            "publication": "guardian",
            "prediction": "no_standalone_investigation",
            "falsification": (
                "Guardian publishes a standalone investigation of Google Gemini's "
                "data handling for Samsung glasses camera footage, with the same "
                "adversarial framing as Kenya/Sama coverage"
            ),
            "timeframe": "within_90_days_of_samsung_launch",
        }
        assert prediction["prediction"] == "no_standalone_investigation"

    def test_prediction_samsung_scandal_diluted_to_category(self):
        """PREDICTION: If a Samsung glasses privacy scandal emerges post-launch,
        the Guardian will frame it as a "smart glasses category" problem, not
        a "Google" problem — diluting entity attribution."""
        prediction = {
            "id": "P83.2",
            "type": "entity_attribution_dilution",
            "trigger": "samsung_glasses_privacy_scandal",
            "expected_guardian_framing": "category_problem",
            "contrast_with_meta_framing": "meta_specific_problem",
            "falsification": (
                "Guardian attributes Samsung glasses scandal specifically to "
                "Google's Gemini AI platform, using 'Google' in headline"
            ),
        }
        assert prediction["expected_guardian_framing"] == "category_problem"

    def test_prediction_no_ico_inquiry_about_gemini_glasses(self):
        """PREDICTION: The Guardian will NOT file FOI requests about Google
        Gemini's processing of Samsung glasses data with the UK ICO, despite
        having demonstrated this investigative technique for OpenAI (Stargate)."""
        prediction = {
            "id": "P83.3",
            "type": "investigative_technique_non_transfer",
            "technique": "foi_request_to_ico",
            "applied_to_openai": True,
            "predicted_for_google_wearables": False,
            "falsification": (
                "Guardian files FOI with ICO about Google Gemini's data "
                "handling for Samsung glasses camera footage"
            ),
        }
        assert prediction["applied_to_openai"]
        assert not prediction["predicted_for_google_wearables"]

    def test_prediction_apple_glasses_aspirational_framing(self):
        """PREDICTION: Apple N50 smart glasses (WWDC 2027) will receive
        aspirational privacy framing from the Guardian, creating a 3-tier
        entity hierarchy:
          Apple: aspirational ("privacy-first")
          Samsung/Google: absent (no coverage)
          Meta: adversarial ("surveillance", "Kenya contractors")

        This hierarchy mirrors Apple News+ revenue flow (Apple pays publishers;
        Google pays Guardian but not for wearables; Meta pays nobody)."""
        prediction = {
            "id": "P83.4",
            "type": "three_tier_entity_hierarchy",
            "apple": "aspirational",
            "samsung_google": "absent",
            "meta": "adversarial",
            "falsification": (
                "Guardian applies identical adversarial framing to Apple N50 "
                "glasses as it does to Meta Ray-Ban"
            ),
        }
        tiers = [prediction["apple"], prediction["samsung_google"], prediction["meta"]]
        assert len(set(tiers)) == 3, "Three distinct tiers"

    def test_prediction_no_milmo_big_tobacco_for_gemini(self):
        """PREDICTION: Dan Milmo will NOT produce a "big tobacco moment" style
        editorial about Google's Gemini processing Samsung glasses camera
        footage, even if functional similarities to Meta's pipeline emerge."""
        prediction = {
            "id": "P83.5",
            "type": "framing_non_transfer",
            "journalist": "dan_milmo",
            "framing": "big_tobacco_moment",
            "applied_to_meta": True,
            "predicted_for_google": False,
            "falsification": (
                "Milmo writes editorial using 'big tobacco' or equivalent "
                "metaphor for Google/Samsung glasses privacy practices"
            ),
        }
        assert prediction["applied_to_meta"]
        assert not prediction["predicted_for_google"]


# ================================================================
# CLASS 10: Cross-Reference Validation
# ================================================================


class TestCrossReferences:
    """Verify this mechanism connects to and extends the existing mechanism
    network without duplicating it."""

    def test_extends_mechanism_59_guardian_dual_role(self, research):
        """Mechanism #83 extends #59 (Guardian Dual-Role Paradox) from
        antitrust/licensing to WEARABLES-SPECIFIC coverage selection."""
        mechanisms = research.get("mechanisms", [])
        m59_exists = any(
            m.get("mechanism_id") == 59 or
            "dual" in str(m.get("mechanism_name", "")).lower()
            for m in mechanisms
        )
        # Verify our mechanism adds a new dimension
        m59_focus = "antitrust_licensing"
        m83_focus = "wearables_privacy_coverage"
        assert m59_focus != m83_focus, (
            "Mechanism #83 covers a different domain (wearables) than #59 (antitrust)"
        )

    def test_parallels_mechanism_77_nyt_samsung_silence(self, research):
        """Mechanism #83 parallels #77 (NYT Samsung silence) but adds the
        Google financial relationship dimension unique to the Guardian."""
        # Mechanisms are stored across aggregate_findings and cross_publication_findings
        all_mechanisms = {}
        for section_key in ["aggregate_findings", "cross_publication_findings"]:
            section = research.get(section_key, {})
            if isinstance(section, dict):
                for k, v in section.items():
                    if isinstance(v, dict) and "mechanism_id" in v:
                        all_mechanisms[v["mechanism_id"]] = k
        assert 77 in all_mechanisms, (
            f"Mechanism #77 should exist in the YAML. Found IDs: {sorted(all_mechanisms.keys())[-15:]}"
        )

    def test_connects_to_mechanism_76_samsung_google_leverage(self, research):
        """Mechanism #76 documents Samsung-Google compound advertiser leverage.
        Mechanism #83 shows this leverage may extend to editorial coverage
        selection at publications with Google financial relationships."""
        all_mechanisms = {}
        for section_key in ["aggregate_findings", "cross_publication_findings"]:
            section = research.get(section_key, {})
            if isinstance(section, dict):
                for k, v in section.items():
                    if isinstance(v, dict) and "mechanism_id" in v:
                        all_mechanisms[v["mechanism_id"]] = k
        assert 76 in all_mechanisms, (
            f"Mechanism #76 should exist in the YAML. Found IDs: {sorted(all_mechanisms.keys())[-15:]}"
        )

    def test_validates_mechanism_80_gizmodo_clean_control(self, research):
        """Gizmodo (zero financial ties) produced standalone Samsung glasses
        coverage including privacy (#80). Guardian (Google financial ties)
        produced zero. This validates the financial-alignment model."""
        all_mechanisms = {}
        for section_key in ["aggregate_findings", "cross_publication_findings"]:
            section = research.get(section_key, {})
            if isinstance(section, dict):
                for k, v in section.items():
                    if isinstance(v, dict) and "mechanism_id" in v:
                        all_mechanisms[v["mechanism_id"]] = k
        assert 80 in all_mechanisms, (
            f"Mechanism #80 should exist in the YAML. Found IDs: {sorted(all_mechanisms.keys())[-15:]}"
        )
        # Gizmodo: no deals, covers Samsung privacy
        # Guardian: Google deal, does NOT cover Samsung privacy
        gizmodo_samsung_coverage = True
        guardian_samsung_coverage = False
        assert gizmodo_samsung_coverage != guardian_samsung_coverage, (
            "Clean-control Gizmodo covers Samsung; financially-tied Guardian does not"
        )

    def test_distinct_from_all_existing_guardian_mechanisms(self):
        """This mechanism is distinct from all 5 existing Guardian test files:
        1. guardian_cross_entity_aug6 — Milmo framing baseline (not wearables)
        2. guardian_google_dual_role_paradox_aug12 — antitrust/licensing (not wearables)
        3. guardian_google_sid_governance_capture_aug10 — governance (not wearables)
        4. guardian_rogue_ai_volume_asymmetry_aug10 — rogue AI volume (not wearables)
        5. guardian_cohere_correction — Cohere correction (not wearables)

        This mechanism adds the WEARABLES dimension to the Guardian profile."""
        existing_guardian_mechanisms = [
            "cross_entity_framing",
            "dual_role_paradox",
            "sid_governance_capture",
            "rogue_ai_volume_asymmetry",
            "cohere_correction",
        ]
        new_mechanism = "samsung_google_glasses_coverage_silence"
        assert new_mechanism not in existing_guardian_mechanisms, (
            "This mechanism covers new ground: wearables-specific coverage selection"
        )
