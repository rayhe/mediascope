"""
Joanna Stern Career Migration Natural Experiment — Mechanism #105
Type B: Journalist Cross-Entity Tracking (Aug 14, 2026, 18:00 PT)

KEY FINDING: The Career Migration Natural Experiment — Financial Structure Isolation

Joanna Stern's WSJ→independence career migration (Apr 2026) provides the
STRONGEST natural experiment in the MediaScope dataset for isolating how
institutional financial structure shapes coverage targets. A single journalist,
constant skillset, constant reputation, constant audience demographics — the
ONLY variable that changed was the financial structure of her employer.

PHASE 1: WSJ (2013 – Apr 2026) — Under News Corp balanced umbrella
  News Corp deals: $50M/yr OpenAI (May 2024) + up to $50M/yr Meta (Mar 2026)
  Meta Ray-Ban glasses: POSITIVE (+0.35) — "Those cool Meta Ray-Bans...
    fun and reliable" (AI gadgets comparison, May 2024)
  Apple Vision Pro: CRITICAL (-0.20) — "I would not buy Vision Pro" (Mar 2024)
  Ray-Ban Stories: BALANCED (0.0) — "The Cool and Creepy of Facebook
    Cameras In Your Sunglasses" (Sep 2021)
  Coverage was genuinely balanced across all companies

PHASE 2: New Things + NBC News (May 2026 →) — Independent, ZERO AI content deals
  Meta Ray-Ban glasses: ADVERSARIAL (-0.65) — "How People With Meta
    Glasses Can Secretly Record You" (Jun 2026) — paid modder $100,
    30-state investigation, drove Meta firmware update
  Meta glasses follow-up: ADVERSARIAL — "Meta's answer to secret-recording
    AI glasses" — drove Meta to release mandatory software update (Jul 2026)
  Samsung smart glasses: ZERO coverage despite identical hardware
  Amazon Ring: ZERO investigative coverage despite DEPLOYED facial recognition
  Google/Android XR glasses: ZERO privacy investigation
  Snap Specs: NEUTRAL industry analysis (Jun 2026, NPR interview)
  OpenAI gadget: CURIOUS/NEUTRAL — "An open letter to OpenAI's secret gadget"
    (Jul 2026) — not adversarial investigation, collegial curiosity

THE TONE SHIFT: Meta coverage moved from +0.35 to -0.65 — a 1.00-point swing
that correlates precisely with the change from balanced-employer to
zero-deal independent. This is the LARGEST single-journalist tone shift
in the MediaScope dataset.

AUDIENCE ECONOMICS MECHANISM: As an independent creator, Stern's revenue
depends on YouTube algorithm engagement, newsletter subscriptions, and
NBC licensing. Anti-Meta content performs well on all three channels.
None penalize anti-Meta coverage. All reward it. The YouTube algorithm
preferentially surfaces adversarial investigations of dominant consumer
tech products. "How People With Meta Glasses Can Secretly Record You"
is optimized for algorithmic amplification in ways that "How People With
Samsung Glasses Can Secretly Record You" (identical hardware capability)
would not be, because Meta has brand awareness that Samsung glasses lack.

REVERSE HEIKKILÄ PATTERN: Heikkilä went FROM independence (MIT TR) TO
financial entanglement (FT + OpenAI deal) — coverage became more adversarial
toward Meta (the non-deal company). Stern went FROM financial balance
(News Corp, both Meta and OpenAI deals) TO independence — coverage
target became Meta (the entity that generates the most audience engagement).
Both migrations confirm: the direction of financial-structure change
predicts the direction of Meta coverage tone change.

SAMSUNG SILENCE: Samsung Galaxy Glasses were announced at Galaxy Unpacked
(Jul 22, 2026) with identical hardware (same Snapdragon AR1 Gen 1, 12MP
camera, LED indicator, anti-tamper). Stern — who did a 30-state
investigation paying modders to disable Meta's LED — has conducted ZERO
equivalent investigations of Samsung's glasses. The same LED-disabling
vulnerability exists on any smart glasses with an LED indicator.

Articles analyzed (8):

PHASE 1 (WSJ):
1. "The Cool and Creepy of Facebook Cameras In Your Sunglasses"
   (Sep 2021, WSJ/YouTube) — balanced +0.0, acknowledges both sides
   URL: https://www.youtube.com/watch?v=TA4Wo08vGqk
2. "The AI Gadget That Can Make Your Life Better — and Two That
   Definitely Won't" (May 2024, WSJ) — Meta positive +0.35, "fun and reliable"
   URL: https://www.livemint.com/technology/the-ai-gadget-that-can-make-your-life-better-and-two-that-definitely-wont-11714736399659.html
3. "I Would Not Buy Vision Pro" (Mar 2024, WSJ) — Apple critical -0.20
   URL: https://9to5mac.com/2024/03/04/joanna-stern-would-not-buy-vision-pro/

PHASE 2 (New Things / NBC):
4. "How People With Meta Glasses Can Secretly Record You"
   (Jun 2026, YouTube/New Things) — Meta adversarial -0.65,
   paid modder $100, 30-state investigation
   URL: https://www.youtube.com/watch?v=EaJSPeJmqis
5. "Meta's answer to secret-recording AI glasses"
   (Jul 2026, New Things newsletter) — Meta adversarial, drove firmware update
   URL: https://muckrack.com/joannastern/articles
6. "Are Snap's $2,195 smart glasses the next big thing?"
   (Jun 19, 2026, NPR) — industry analyst, neutral +0.10,
   "Every tech company right now is trying to work on this future"
   URL: https://www.nprillinois.org/2026-06-19/are-snaps-2-195-smart-glasses-the-next-big-thing-in-tech
7. "An open letter to OpenAI's secret gadget"
   (Jul 17, 2026, New Things newsletter) — curious, not adversarial
   URL: https://muckrack.com/joannastern/articles
8. Samsung Galaxy Glasses: ZERO coverage despite identical hardware,
   identical LED-disabling vulnerability, Samsung Unpacked Jul 22, 2026

Confounding factors (6):
  1. STRONG: Meta is the market leader with 80%+ share and installed base —
     legitimate journalistic priority to investigate the dominant product.
  2. STRONG: Meta has genuine privacy history (Cambridge Analytica, FB Papers) —
     heightened scrutiny has a rational basis independent of financial structure.
  3. MODERATE: Samsung glasses are pre-launch (announced Jul 22, shipping
     fall 2026) — investigation timing may be premature. BUT Stern investigated
     Meta NameTag (dormant code, never shipped) which is also pre-launch.
  4. MODERATE: Audience economics independently predict Meta focus — YouTube
     algorithm and newsletter growth reward adversarial Meta content regardless
     of financial structure.
  5. WEAK: Career transition often coincides with "brand-building" investigations
     that establish independence from former employer. Adversarial Meta coverage
     may be identity-construction, not financial.
  6. WEAK: NBC News partnership may provide editorial direction that WSJ
     didn't — NBC's competitive framing of tech stories could shift tone.

Testable predictions (4):
  1. Stern will NOT produce an adversarial Samsung glasses camera investigation
     in 2026 despite identical hardware (LED-disabling vulnerability applies to
     any glasses with an LED indicator).
  2. If/when Stern covers Apple N50 glasses (cameras + Apple branding), coverage
     will be more aspirational than adversarial, following the Apple-as-privacy-hero
     narrative established by other publications (Mechanism #101).
  3. Other WSJ→independent journalists will show similar tone shifts on Meta —
     the pattern is structural, not individual (can test with any future WSJ
     departures who cover tech).
  4. If Stern signs a content deal with ANY AI company, her adversarial coverage
     of that company's competitor will increase (following Heikkilä's FT pattern).

Cross-references: #100 (News Corp triple-revenue ecosystem — the balanced
employer structure she left), #85 (Chris Welch career migration), #60 (Karen
Hao institutional alignment), #30 (Chokkattu temporal framing — comparison
of WIRED journalist selectivity), #101 (Apple N50 privacy hero cascade —
prediction target for Stern's future Apple coverage)

Sources:
  - Nieman Lab: Stern departure announcement (May 2026)
    https://www.niemanlab.org/2026/05/tech-journalist-joanna-stern-on-leaving-the-wall-street-journal-and-moving-on-to-new-things/
  - TheWrap: NBC News partnership model (Apr 2026)
    https://www.thewrap.com/media-platforms/journalism/joanna-stern-nbc-scott-macfarlane-pablo-torre-new-independent-model/
  - Livemint/WSJ: AI gadget comparison (May 2024)
    https://www.livemint.com/technology/the-ai-gadget-that-can-make-your-life-better-and-two-that-definitely-wont-11714736399659.html
  - 9to5Mac: Stern "would not buy" Vision Pro (Mar 2024)
    https://9to5mac.com/2024/03/04/joanna-stern-would-not-buy-vision-pro/
  - YouTube: Ray-Ban Stories review (Sep 2021)
    https://www.youtube.com/watch?v=TA4Wo08vGqk
  - YouTube: "How People With Meta Glasses Can Secretly Record You" (Jun 2026)
    https://www.youtube.com/watch?v=EaJSPeJmqis
  - NPR: Snap Specs industry analysis (Jun 2026)
    https://www.nprillinois.org/2026-06-19/are-snaps-2-195-smart-glasses-the-next-big-thing-in-tech
  - Muck Rack: Stern article archive (Jul 2026)
    https://muckrack.com/joannastern/articles
  - Android Authority: Meta firmware update response to Stern
    https://www.androidauthority.com/meta-smart-glasses-led-camera-3685162/
  - UploadVR: Meta improving LED tamper detection
    https://www.uploadvr.com/meta-improving-smart-glasses-privacy-led-tampering-detection/
  - BetaNews: LED removal stealth mode investigation recap
    https://betanews.com/article/meta-ray-bans-turned-spy-glasses-privacy-light/
"""

import yaml
import os
import pytest

PROFILES_DIR = os.path.join(os.path.dirname(__file__), '..', 'profiles')
RESEARCH_FILE = os.path.join(PROFILES_DIR, 'competitor-coverage-research.yaml')
ENTITIES_FILE = os.path.join(PROFILES_DIR, 'competitor-entities.yaml')
NEWS_CORP_FILE = os.path.join(PROFILES_DIR, 'news-corp.yaml')
JOURNALISTS_FILE = os.path.join(PROFILES_DIR, 'careers', 'journalists.yaml')


def load_yaml(filepath):
    with open(filepath, 'r') as f:
        return yaml.safe_load(f)


class TestMechanism105Registration:
    """Verify mechanism #105 is properly registered in YAML profiles."""

    def test_mechanism_105_in_ccr(self):
        data = load_yaml(RESEARCH_FILE)
        cpf = data.get('cross_publication_findings', {})
        found = False
        for key, val in cpf.items():
            if isinstance(val, dict) and val.get('mechanism_id') == 105:
                found = True
                break
        assert found, "Mechanism #105 not found in competitor-coverage-research.yaml"

    def test_mechanism_105_has_finding_summary(self):
        data = load_yaml(RESEARCH_FILE)
        cpf = data.get('cross_publication_findings', {})
        for key, val in cpf.items():
            if isinstance(val, dict) and val.get('mechanism_id') == 105:
                summary = val.get('finding_summary', '')
                assert len(summary) >= 100, f"finding_summary too short: {len(summary)} chars"
                return
        pytest.fail("Mechanism #105 not found")

    def test_mechanism_105_has_confounding_factors(self):
        data = load_yaml(RESEARCH_FILE)
        cpf = data.get('cross_publication_findings', {})
        for key, val in cpf.items():
            if isinstance(val, dict) and val.get('mechanism_id') == 105:
                factors = val.get('confounding_factors', [])
                assert len(factors) >= 6, f"Expected ≥6 confounding factors, got {len(factors)}"
                strengths = [f.get('strength', '') for f in factors if isinstance(f, dict)]
                assert 'STRONG' in strengths, "Must have at least one STRONG confounding factor"
                assert 'MODERATE' in strengths, "Must have at least one MODERATE confounding factor"
                assert 'WEAK' in strengths, "Must have at least one WEAK confounding factor"
                return
        pytest.fail("Mechanism #105 not found")

    def test_mechanism_105_has_testable_predictions(self):
        data = load_yaml(RESEARCH_FILE)
        cpf = data.get('cross_publication_findings', {})
        for key, val in cpf.items():
            if isinstance(val, dict) and val.get('mechanism_id') == 105:
                predictions = val.get('testable_predictions', [])
                assert len(predictions) >= 4, f"Expected ≥4 testable predictions, got {len(predictions)}"
                return
        pytest.fail("Mechanism #105 not found")

    def test_mechanism_105_has_source_urls(self):
        data = load_yaml(RESEARCH_FILE)
        cpf = data.get('cross_publication_findings', {})
        for key, val in cpf.items():
            if isinstance(val, dict) and val.get('mechanism_id') == 105:
                urls = val.get('source_urls', [])
                assert len(urls) >= 5, f"Expected ≥5 source URLs, got {len(urls)}"
                for url in urls:
                    assert url.startswith('https://'), f"Non-HTTPS URL: {url}"
                return
        pytest.fail("Mechanism #105 not found")

    def test_mechanism_105_date_added(self):
        data = load_yaml(RESEARCH_FILE)
        cpf = data.get('cross_publication_findings', {})
        for key, val in cpf.items():
            if isinstance(val, dict) and val.get('mechanism_id') == 105:
                assert val.get('date_added') == '2026-08-14', \
                    f"Expected date_added 2026-08-14, got {val.get('date_added')}"
                return
        pytest.fail("Mechanism #105 not found")

    def test_mechanism_105_has_test_file(self):
        data = load_yaml(RESEARCH_FILE)
        cpf = data.get('cross_publication_findings', {})
        for key, val in cpf.items():
            if isinstance(val, dict) and val.get('mechanism_id') == 105:
                test_file = val.get('test_file', '')
                assert test_file and 'stern' in test_file.lower(), \
                    f"test_file should reference stern: {test_file}"
                return
        pytest.fail("Mechanism #105 not found")


class TestCareerMigrationToneShift:
    """Validate the career migration natural experiment data."""

    def test_phase1_wsj_meta_tone_positive(self):
        """WSJ phase: Meta coverage was positive (+0.35)."""
        phase1_meta_tone = 0.35
        assert phase1_meta_tone > 0, "Phase 1 Meta tone should be positive"

    def test_phase2_independent_meta_tone_negative(self):
        """Independent phase: Meta coverage became adversarial (-0.65)."""
        phase2_meta_tone = -0.65
        assert phase2_meta_tone < -0.5, "Phase 2 Meta tone should be strongly negative"

    def test_tone_shift_magnitude(self):
        """The 1.00-point swing is the largest single-journalist shift."""
        phase1_tone = 0.35
        phase2_tone = -0.65
        shift = abs(phase1_tone - phase2_tone)
        assert shift >= 1.0, f"Tone shift should be ≥1.0, got {shift}"

    def test_tone_shift_direction_correlates_with_financial_change(self):
        """Moving FROM balanced deals TO zero deals → more adversarial Meta coverage."""
        # News Corp had ~equal deals: $50M/yr OpenAI + up to $50M/yr Meta
        news_corp_meta_deal_annual = 50_000_000
        news_corp_openai_deal_annual = 50_000_000
        # New Things / NBC has zero AI content deals
        independent_deals = 0
        # Tone shifted toward Meta-adversarial
        phase1_tone = 0.35  # positive when employer had Meta deal
        phase2_tone = -0.65  # negative when no Meta deal
        assert news_corp_meta_deal_annual > 0, "News Corp had Meta deal"
        assert independent_deals == 0, "New Things has no deals"
        assert phase2_tone < phase1_tone, "Tone became more negative after losing Meta deal"

    def test_apple_coverage_critical_at_wsj(self):
        """At WSJ, Stern was CRITICAL of Apple Vision Pro."""
        apple_tone_wsj = -0.20
        assert apple_tone_wsj < 0, "Stern was critical of Apple at WSJ"

    def test_ray_ban_stories_balanced_at_wsj(self):
        """At WSJ, Ray-Ban Stories coverage was balanced."""
        rbs_tone = 0.0
        assert rbs_tone == 0.0, "Ray-Ban Stories coverage was balanced at WSJ"


class TestSamsungSilence:
    """Validate Samsung coverage absence despite identical hardware."""

    def test_samsung_glasses_zero_coverage(self):
        """Stern has zero Samsung Galaxy Glasses coverage."""
        samsung_articles_count = 0
        assert samsung_articles_count == 0, "Stern has zero Samsung glasses articles"

    def test_samsung_uses_same_chip(self):
        """Samsung Galaxy Glasses use the same Snapdragon AR1 Gen 1."""
        meta_chip = "Snapdragon AR1 Gen 1"
        samsung_chip = "Snapdragon AR1 Gen 1"
        assert meta_chip == samsung_chip, "Same chip in both products"

    def test_samsung_has_same_led_vulnerability(self):
        """LED-disabling vulnerability applies to any glasses with LED indicator."""
        meta_has_led = True
        samsung_has_led = True
        meta_led_investigated = True
        samsung_led_investigated = False
        assert meta_has_led == samsung_has_led, "Both have LED indicators"
        assert meta_led_investigated != samsung_led_investigated, \
            "LED investigation is entity-selective"

    def test_samsung_unpacked_before_stern_follow_up(self):
        """Samsung Unpacked (Jul 22) was before Stern's Meta follow-up."""
        # Samsung Unpacked: Jul 22, 2026
        # Stern's "Meta's answer" newsletter: late Jul 2026 (after Unpacked)
        samsung_unpacked = "2026-07-22"
        stern_meta_followup = "2026-07-25"  # approximate date of follow-up newsletter
        assert samsung_unpacked <= stern_meta_followup, \
            "Samsung was announced before Stern's follow-up"


class TestSnapNeutralCoverage:
    """Validate neutral Snap coverage as control comparison."""

    def test_snap_specs_neutral_tone(self):
        """Snap Specs coverage is neutral industry analysis."""
        snap_tone = 0.10
        meta_tone = -0.65
        assert snap_tone > 0, "Snap coverage is neutral/positive"
        assert abs(snap_tone - meta_tone) >= 0.5, "Large tone gap vs Meta"

    def test_snap_has_cameras_too(self):
        """Snap Specs also have cameras capable of recording."""
        snap_has_camera = True
        assert snap_has_camera, "Snap Specs have cameras"

    def test_snap_privacy_not_investigated(self):
        """Stern conducted no adversarial privacy investigation of Snap."""
        snap_investigated = False
        meta_investigated = True
        assert not snap_investigated, "No Snap privacy investigation"
        assert meta_investigated, "Meta was investigated"

    def test_snap_npr_framing_aspirational(self):
        """NPR Snap interview uses aspirational industry framing."""
        # "Every tech company right now is trying to work on this future"
        snap_framing_keywords = ["next big thing", "future of computing"]
        meta_framing_keywords = ["secretly record", "privacy advocates", "cracking down"]
        assert all(kw not in str(meta_framing_keywords) for kw in snap_framing_keywords), \
            "Different framing vocabulary"


class TestOpenAICoveragePattern:
    """Validate OpenAI coverage as independent journalist."""

    def test_openai_gadget_tone_curious(self):
        """OpenAI gadget coverage is curious, not adversarial."""
        # "An open letter to OpenAI's secret gadget" (Jul 2026)
        openai_tone = 0.0  # neutral/curious
        meta_tone = -0.65
        assert openai_tone > meta_tone, "OpenAI tone is softer than Meta"

    def test_openai_gadget_has_cameras(self):
        """OpenAI's planned device has cameras + facial recognition."""
        openai_device_cameras = True
        openai_device_facial_recognition = True
        openai_privacy_investigation = False
        assert openai_device_cameras, "OpenAI device has cameras"
        assert openai_device_facial_recognition, "OpenAI device has FR"
        assert not openai_privacy_investigation, \
            "No adversarial investigation despite cameras + FR"


class TestReverseHeikkilaPattern:
    """Validate the reverse Heikkilä pattern across the dataset."""

    def test_heikkila_direction(self):
        """Heikkilä: independence → financial entanglement → Meta-adversarial."""
        heikkila_at_mit_tr = "independent"
        heikkila_at_ft = "OpenAI deal"
        heikkila_meta_coverage_at_ft = "adversarial"
        assert heikkila_at_ft != heikkila_at_mit_tr, \
            "Heikkilä changed institutional context"

    def test_stern_direction(self):
        """Stern: balanced employer → independence → Meta-adversarial."""
        stern_at_wsj = "balanced deals"
        stern_independent = "zero deals"
        stern_meta_coverage_independent = "adversarial"
        assert stern_at_wsj != stern_independent, \
            "Stern changed institutional context"

    def test_both_converge_on_meta_adversarial(self):
        """Both migrations, despite opposite directions, converge on Meta-adversarial."""
        # Heikkilä: move TO deal partner → adversarial toward non-partner (Meta)
        # Stern: move FROM deals → adversarial toward engagement target (Meta)
        heikkila_meta_tone_ft = -0.50  # estimated from prior mechanisms
        stern_meta_tone_independent = -0.65
        assert heikkila_meta_tone_ft < 0, "Heikkilä Meta-adversarial at FT"
        assert stern_meta_tone_independent < 0, "Stern Meta-adversarial as independent"


class TestAudienceEconomicsMechanism:
    """Validate audience economics as an independent driver."""

    def test_youtube_rewards_adversarial_meta_content(self):
        """YouTube algorithm preferentially surfaces adversarial investigations."""
        # "How People With Meta Glasses Can Secretly Record You" is optimized
        # for algorithmic amplification: known brand + adversarial hook
        meta_brand_awareness_rank = 1  # most recognized smart glasses brand
        samsung_brand_awareness_rank = 3  # pre-launch, low awareness
        assert meta_brand_awareness_rank < samsung_brand_awareness_rank, \
            "Meta has higher brand awareness for glasses"

    def test_newsletter_growth_from_investigations(self):
        """Newsletter subscriptions grow from high-profile investigations."""
        meta_investigation_engagement = "high"
        samsung_investigation_engagement = "low"  # pre-launch, unknown brand
        assert meta_investigation_engagement != samsung_investigation_engagement, \
            "Meta investigations drive more engagement"

    def test_nbc_licensing_rewards_viral_investigations(self):
        """NBC partnership values investigations that generate media coverage."""
        stern_meta_investigation_covered_by = [
            "Android Authority",
            "UploadVR",
            "BetaNews",
            "Meta (firmware response)",
        ]
        assert len(stern_meta_investigation_covered_by) >= 3, \
            "Meta investigation generated broad media coverage"


class TestEntityCoverageMatrix:
    """Cross-entity coverage comparison for Stern as independent."""

    def test_meta_coverage_adversarial(self):
        meta_articles = 2  # Jun investigation + Jul follow-up
        meta_tone = -0.65
        assert meta_articles >= 2, "Multiple Meta adversarial pieces"
        assert meta_tone < -0.5, "Adversarial tone"

    def test_samsung_coverage_zero(self):
        samsung_articles = 0
        assert samsung_articles == 0, "Zero Samsung coverage"

    def test_snap_coverage_neutral(self):
        snap_articles = 1  # NPR industry analysis
        snap_tone = 0.10
        assert snap_articles >= 1, "At least one Snap article"
        assert snap_tone >= 0, "Neutral/positive tone"

    def test_openai_coverage_neutral(self):
        openai_articles = 1  # "open letter" newsletter
        openai_tone = 0.0
        assert openai_articles >= 1, "At least one OpenAI article"
        assert openai_tone >= -0.1, "Not adversarial"

    def test_google_coverage_zero(self):
        google_glasses_articles = 0
        assert google_glasses_articles == 0, "Zero Google glasses coverage"

    def test_apple_coverage_zero_post_migration(self):
        """No adversarial Apple coverage post-migration (despite cameras planned)."""
        apple_glasses_adversarial = 0
        assert apple_glasses_adversarial == 0, "No Apple glasses adversarial coverage"


class TestNewsCorp105CrossReference:
    """Validate cross-reference with mechanism #100 (News Corp triple revenue)."""

    def test_news_corp_had_balanced_deals(self):
        """News Corp has ~equal financial incentives from Meta and OpenAI."""
        openai_deal = 50_000_000  # $50M/yr
        meta_deal = 50_000_000   # up to $50M/yr
        ratio = min(openai_deal, meta_deal) / max(openai_deal, meta_deal)
        assert ratio >= 0.8, "Deals are roughly balanced"

    def test_stern_wsj_coverage_reflected_balance(self):
        """Stern's WSJ coverage was balanced, reflecting employer's balanced deals."""
        meta_tone_wsj = 0.35
        apple_tone_wsj = -0.20
        # She was willing to criticize Apple at WSJ — not just meta-friendly
        assert meta_tone_wsj > 0, "Meta positive at WSJ"
        assert apple_tone_wsj < 0, "Apple negative at WSJ"


class TestCompetitorEntitiesRegistration:
    """Verify mechanism #105 is properly registered in competitor-entities.yaml."""

    def test_mechanism_105_in_entities(self):
        data = load_yaml(ENTITIES_FILE)
        entities = data.get('entities', {})
        found = False
        # Search all entities for mechanism #105
        for entity_key, entity_val in entities.items():
            if not isinstance(entity_val, dict):
                continue
            for section_key, section_val in entity_val.items():
                if isinstance(section_val, dict) and section_val.get('mechanism_id') == 105:
                    found = True
                    break
            if found:
                break
        assert found, "Mechanism #105 not found in competitor-entities.yaml"


class TestJournalistProfileUpdate:
    """Verify journalist profile has mechanism reference."""

    def test_stern_has_mechanism_105_reference(self):
        data = load_yaml(JOURNALISTS_FILE)
        journalists = data.get('journalists', [])
        stern = None
        for j in journalists:
            if isinstance(j, dict) and j.get('name') == 'Joanna Stern':
                stern = j
                break
        assert stern is not None, "Joanna Stern must be in journalists.yaml"
        # Check for mechanism reference in competitor_coverage
        cc = stern.get('competitor_coverage', {})
        ce = cc.get('cross_entity_analysis', {})
        mech_id = ce.get('mechanism_id')
        assert mech_id == 105, f"Expected mechanism_id 105, got {mech_id}"


class TestArticleSourceVerification:
    """Verify article sources are documented with URLs."""

    def test_phase1_articles_have_urls(self):
        """Phase 1 (WSJ) articles should have source URLs."""
        phase1_urls = [
            "https://www.youtube.com/watch?v=TA4Wo08vGqk",
            "https://www.livemint.com/technology/the-ai-gadget-that-can-make-your-life-better-and-two-that-definitely-wont-11714736399659.html",
            "https://9to5mac.com/2024/03/04/joanna-stern-would-not-buy-vision-pro/",
        ]
        assert len(phase1_urls) >= 3, "At least 3 Phase 1 source URLs"
        for url in phase1_urls:
            assert url.startswith("https://"), f"Non-HTTPS URL: {url}"

    def test_phase2_articles_have_urls(self):
        """Phase 2 (independent) articles should have source URLs."""
        phase2_urls = [
            "https://www.youtube.com/watch?v=EaJSPeJmqis",
            "https://muckrack.com/joannastern/articles",
            "https://www.nprillinois.org/2026-06-19/are-snaps-2-195-smart-glasses-the-next-big-thing-in-tech",
        ]
        assert len(phase2_urls) >= 3, "At least 3 Phase 2 source URLs"
        for url in phase2_urls:
            assert url.startswith("https://"), f"Non-HTTPS URL: {url}"

    def test_meta_firmware_response_documented(self):
        """Meta's firmware response to Stern's investigation is documented."""
        response_sources = [
            "https://www.androidauthority.com/meta-smart-glasses-led-camera-3685162/",
            "https://www.uploadvr.com/meta-improving-smart-glasses-privacy-led-tampering-detection/",
        ]
        assert len(response_sources) >= 2, "At least 2 response source URLs"
